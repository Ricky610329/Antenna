# Fanout 審視存檔（2026-07-29,14×Opus,29 發現→前 8 對抗驗證）

> 一次性多代理審視（Ricky 授權）。[now] 級 5 件已當日修畢（commit 9c87411）。
> 本檔=完整結果濃縮存檔;unverified 條目**未經對抗驗證**,採用前需自行查證。

## 確認（7,含驗證者修訂）

### [now|S|toolchain] 昨天為防呆寫進 scratch 的 lo 欄位註記本身把兩個欄位寫反了
- **位置**: docs/discuss/scratch.md:588（「`contrast_lo`（usable 對比口徑,紀錄 usable_lo 用）」）vs script/analyze.py:1005 與 1086-1089（usable_lo 紀錄候選實際讀 `oob_gain_max_lo`）、script/dedust.py:605-633 `oob_metrics` docstring
- **驗證判詞（節錄）**: 事實三腳全部求證通過,無誇大。①程式碼:script/analyze.py:1005 `lo=r.get("oob_gain_max_lo")`,:1086-1089 usable_lo/hi 紀錄候選迴圈讀 s[side] → 紀錄鍵確實用 oob_gain_max_*;script/dedust.py:4581-4586 _chain_score goal=lo/hi 讀 oob_gain_max_{goal},goal=tri/rad 用 `lo <= -2.0` 門檻。②全 repo grep:contrast_lo 只出現在 dedust.py:625（定義）與 tests/test_dedust.py:253-263（值斷言）——不進任何紀錄鍵、鏈鍵、analyze 路徑。③NAS 實測（唯讀）:c8trip03_01 contrast_lo=6.79/oob_gain_max_lo=-2.63;c41grp2p02_02 contrast_lo=7.61/oob_gain_max_lo=-3.46;docs/records.json usable_lo=-3.46(c41grp2p02_02)、usable_oob cert 文字寫 "lo-2.630"、usable_hi cert 明寫 "min oob_gain_max_hi" → 基線全部來自 oob_gain
- **修訂後行動**: 兩處純文件修正,合計 S（<15 分鐘）,零行為變更:  ①`docs/discuss/scratch.md:588`——把括號改成「`contrast_lo`（相對選擇性追蹤欄,**不進任何紀錄鍵/鏈鍵**）」;並把該條粗體規則從只講鏈線擴為涵蓋紀錄鍵:「紀錄鍵 usable_lo/hi 與鏈鍵 goal=lo/hi/tri 一律用 `oob_gain_max_*`」。依 CLAUDE.md scratch 生命週期規範,這是改錯不是否決,原條目其餘文字（c47d1 事故經過、9 dB 差距實例、工具修正候選）保留不刪。  ②`script/dedust.py` `oob_metrics` docstring（約 605-612 行的說明段）——不要只在末尾追加,必須同時修掉已過時的「判準仍只用 oob_bad…其餘為壓帶外戰役的追蹤欄」該句（它把分側 _lo/_hi 也算成追蹤欄,自 2026-07-23 左右拆帳制起為假)。建議改寫成三分法一句話:「`oob_bad`=總帳判準;`oob_gain_max_lo/hi`=**絕對 Gain**,即紀錄鍵 usable_lo/hi 與鏈鍵 goal=lo/hi/tri 門檻所用（見 decisions「左右側拆帳紀錄制」）;`contrast_*`/`rolloff_*`/`oob_gain_argmax`=追蹤欄,不進任何鍵。」——指向 decisions 而非複製整套制度,守「指向不複製」。  不要順手做的:scratch:588 已列的「chain 啟動驗錨 goal 鍵值,盆地外 refuse 發車」是真正的根因修復（每次事故省 ~50 筆 HFSS),但那是動生產中工具的 code 改動,必須另案處理並附回歸測試（S-M),不可夾帶進這次文件修正。  驗證方式:改完 grep 一次 `contrast_lo` 確認全 repo 僅剩 dedust.py 定義、tests 斷言、scratch/docstring 的「不進任何鍵」敘述;不需跑 pytest（無程式行為變更,但若要保險 tests/test_d

### [now|S|toolchain] v86 的配套模型缺三顆，select 會靜默關掉三個「已進鍵」的選拔機制
- **位置**: script/dedust.py:3843-3901（select_r22mix，select-r40~r47 共用）＋ 3335-3368（L 臂）；NAS 現況：sm_reanchor86.pth/rad_head86.pth/sm_ens86_* 存在，sm_two86.pth / sm_shadow86.pth / sm_lohead86.pth **全部不存在**
- **驗證判詞（節錄）**: 機制成立、結論成立，但原提案三根事實支柱有兩根是錯的，且它引用的證據比真正的證據弱。逐條查證（全唯讀）：  【✅ 確認為真】靜默降級機制。script/dedust.py:3841-3901（select_r22mix）與 3335-3368（L 臂）確實用 `_vn = args.sm 的數字` 字串配對配套檔，全部是 `if _f.exists():` 有才用、**print 只在成功分支內**（3858 ens／3893 two／3900 lohead／3901-3903 cnn），缺件零輸出。降級路徑也確認：4056 `and any(c.get("pred_wm_cnn") is not None ...)` 整段守衛失敗 → O 臂退回純 `_psel`；4063 `if any(pred_wm_two ...)` 否則退 cnn/mlp；4043 `wm_lcb = c["pred_wm"] - c.get("pred_std", 0.0)` → **ens 缺件時 LCB 退化成裸預測，R31b2 進鍵的保守性整批消失**（這條原提案沒抓到，比它列的三顆更貴，因為 std 是無條件進鍵、不需 batch>=2）。  【❌ 事實錯誤 1：manifest】原提案說「manifest 完全不存配套欄位、事後無法回查」——**錯，它看錯檔**。它驗的 `tmp/inver
- **修訂後行動**: 三刀，全部 S（合計 <1hr），按報酬排序；不加旗標、不加 exit 1、不加 header entry：  ① **select 開場印一行配套解析結果**（script/dedust.py:3841 附近，`_vn` 算完後、各 `if _f.exists()` 之前）：把 two/cnn/lohead/ens/rad_head 五項各標「✅vNN」或「⚠缺→<鍵名>停用」，成功與缺件都印。約 6 行，取代現有分散在 3858/3893/3900/3901 的四個成功分支 print（**淨減行數**，符合能刪不要包）。務必含 ens→「pred_std 缺→LCB 退化成裸預測」，這條原提案漏了且最貴。  ② **把解析結果寫進 manifest 每筆列**（dedust.py:4176 的 `entries.append(dict(...))` 加兩個 key）：`sm=os.path.basename(args.sm)`、`heads="two86,cnn86,lo86,ens86x2,rad86"`（缺件寫 `-`）。**加在每筆列、不要加夾層 header**——manifest 是 list，混形會弄壞 analyze/report/check-dup 全部 reader。這是 scratch 2026-07-23 健檢④「靜默降級要記帳欄位不只 print」的落點；現有 pred_* null 已能回查「哪些鍵活著」，補的是「哪一顆」。  ③ **`--rad-head` 預設去硬編**（6688 及後續新 select-rNN）：`default=None`，執行時由 `--sm` 數字推 `rad_head{vn}.pth`，不存在才退最新既有並印⚠。刪掉未來每輪手抄一次字面值的來源。  ④ **同步修 .claude/skills/batch-cycle/SKILL.md 步驟③**：補上 `sm_reanchor train-two --out sm_reanchor<NN+1>.pth`（目前完全沒有，照字面走就會在 two/lohead 

### [now|S|methodology] 「d/g 兩條血統都停在 −2.7~−3.0」與帳不符——牆其實只有 n=1
- **位置**: round-46-relay-2.md §4/§5-1、round-47-relay-3.md §1 背景與判準、scratch 2026-07-28/07-29 高原條件① 條目；對照真相源 docs/chains/c45d1.jsonl / c45d2.jsonl
- **驗證判詞（節錄）**: 事實查核全部對上,且比提案更嚴重一點(錯誤自我矛盾且已擴散到 scratch)。  【真相源】docs/chains/c45d1.jsonl 三包:anchor −9.22 → p01 −9.18 → p02 −7.24 → p03 −7.15(wm −7.00);docs/chains/c45d2.jsonl 兩包 win:false(−7.18/−7.16,wm −7.03/−7.01)。d 線終點 = score −7.15 / wm −7.00,dry2 收於 −7 帶。g 線(c45g1→g4)終點 c45g3p02_06 wm −2.72。兩者差 4.3 dB,提案數字正確。  【錯誤位置(逐條確認)】 - round-46-relay-2.md:48 「與 d 線同型(高原帶 −2.7~−3.0)但爬得遠得多(+5.32 vs +2.07)」——同一句內的 +2.07 正是 −9.22→−7.15,**句內自相矛盾**,可獨立佐證是誤植而非口徑差異。 - round-46-relay-2.md:58 §5-1「兩條血統(d/g)都停在 −2.7~−3.0 帶——最後 1.7 dB 是結構性的牆」——無歧義的錯誤陳述。 - round-47-relay-3.md:10 §1 背景「d 線(+2.07)與 g 線(+5.32)雙雙止步 −2.7~−3.0 帶」+ :18 
- **修訂後行動**: 照提案在三處補「★ 修正(2026-07-29)」,但改兩點措辭:  1. round-46-relay-2.md §4(:48)與 §5-1(:58) 各補一條:「★ 修正(2026-07-29):d 線終點為 wm −7.00(chain score −7.15,dry2 收於 −7 帶),來源 docs/chains/c45d1.jsonl + c45d2.jsonl,非 −2.7~−3.0 帶(同句 +2.07 即由 −9.22 推得,原文自相矛盾)。故『苗子帶→作戰區最後 1.72 dB 的牆』目前為 g 系單一觀測(n=1),d 線撞的是另一道 −7 帶牆,對此牆零證據力。R45 §4/§5-2 原文正確,誤植始於本輪。」原文保留不刪。  2. round-47-relay-3.md §1 補一條「★ 修正(2026-07-29 12:4x,**p01/p02 結果已回**,誠實聲明)」:①背景訂正為「g 線單一血統止於 −2.72;d 線止於 −7.00=不同的牆」;②判準文字「第三血統」改「第二條嘗試該段的血統」;③**判準門檻本體(破 −2.7=假說 A;停同帶=假說 B 增證)一字不動**——只註明「屆時證據強度為 n=2 的案例級,不足以單獨把 GNN bakeoff 的優先級拉前,升優先與否併 Ricky 判」。理由:硬紀律1 的修訂窗口已因 c47d2 兩包回檔而關閉,不得聲稱「發生在結果前」;維持門檻不動即不構成事後改判準。  3. docs/discuss/scratch.md:587(07-29 高原條件① 條)就地標「★ 修正」同上;:585(07-28 條)**不要動**——該條原文正確。  4. 順手(同一次編輯,零額外成本):configs/ONGOING.md:27 R47 行仍寫「d46b3_008 擇錨新鏈」,實際錨已因 c47d1 廢鏈換成 g46b3_000_free_randf——live 操作板對接手者是誤導,一併更新。  不需要動 docs/log/README.md:41、configs/ONGOING.md:2

### [now|S|knowledge] champions.md 落後 records.json 兩次已公證換王；且該漂移已被寫作 session 登記為 ⏸「不改」，卻沒路由回擁有者
- **位置**: C:\Users\Ricky\Documents\GitHub\Antenna\docs\champions.md（頭銜表 margin 王 o29b2_011 +0.56、無 usable_lo 列，標「最後更新 2026-07-24」）vs docs\records.json（wm m42b1_003… +0.73 公證 07-26、usable_lo c41grp2p02_02 −3.46 公證 07-25）；待辦登記在 docs\methodology-divergences.md §三 F4-F7
- **驗證判詞（節錄）**: 核心事實全部查證屬實，但範圍被低估、風險被高估一處、②③兩個行動不符專案哲學。  【屬實】 1. champions.md 頭銜表 margin 王仍 o29b2_011 +0.56、現任榜 rank −1 同筆、無 usable_lo −3.46 列（lo −2.63 併在 🏆🌊 c8trip03_01 那列）；標頭「最後更新 2026-07-24，R38」。records.json：wm=m42b1_003… 0.73（公證 07-26）、usable_lo=c41grp2p02_02 −3.46（公證 07-25）。漂移 0.17 dB ＋一整個紀錄軸，屬實。 2. 「連兩次沒執行」有 git 級證據：3710161（usable_lo）與 39dc9e1（wm 王）兩個 commit 的 --stat 都只含 records.json ＋ round 檔 ＋ newking png，champions.md 零改動。 3. F4 登記在 docs/methodology-divergences.md §三，標 ⏸「不改（批次線 session 地盤／對外資產）」；該檔確實不在批次線讀的四層（log／ONGOING／discuss／records）任何一層 → 「已知漂移、無人認領」成立。F5/F6/F7 同狀態。 4. 零風險：grep 全 repo，沒有任何 .py 讀 
- **修訂後行動**: 縮小＋改向，四步，全部 S： ① 由批次線 session（champions.md/memory 的 de facto 寫者）一次補完 R41n1＋r42n2a 的記帳後半段：champions.md 頭銜表 margin 王改 m42b1_003 +0.73（前任 o29b2_011 +0.56 降級註記、在位 10 天）＋現任榜 rank −1 換行、🏆🌊 那列 lo 改 −3.46（c41grp2p02_02）或另立 usable_lo 列、標頭日期改今天。 ② 同批補 /notarize step 5：docs/memory/project_w17_champion.md 內文（停在 R29b2）＋ MEMORY.md 索引行的 margin 王／lo 數字。**這步優先於 ①**——它是唯一會被 auto-inject 進每個 session 的過期數字。 ③ 不加第四行 checklist（/reconcile 已有同一自檢）。改在 champions.md 開頭加一行時效聲明：「數字為散文快照，以 docs/records.json 為準；本檔獨有＝血統鏈／結構定律／頭銜判準」——讓漂移自我宣告，讀者無論有沒有人記得 checklist 都會被路由對。 ④ 誠實標出真正的化石：在 champions.md「血統」「歷史對比圖」「侷限與進行中」三節各加一句 `<!-- 停在 R20，待重寫 -->`（L72「現任 margin 王＝r2_016」／L97 血統鏈止於 R19-R20／L123「進行中:R19 模型線籌備」）。這三節是 records.json 沒有的那層，也是 8 月對外文件真正會誤引的地方；全面重寫是 M 且需要 Ricky 過帳，本次只標不改。 不做：F4-F7 進 ONGOING 🔜 候選區（該區是研究點子佇列不是雜務台帳，且自身已是同一失敗模式的展示櫃）。F5/F6/F7 留在 methodology-divergences.md ⏸，由 Ricky／寫作線認領；若要給它們一個擁有者，正確位置是在 handoff-direction

### [soon|S|core] 護欄有洞：golden 只保 run_training 的數值軌跡，「zoo 建得起來」與「SM 訓練健康度」兩條都沒有任何測試把關——前兩個發現都是在 CI 全綠下存活的
- **位置**: tests/（test_cnn_surrogate.py:39-44 直接 new CNNSurrogate、繞過 build_surrogate；test_surrogate_training.py 只驗 NaN guard 與廣播警告；test_ensemble_trust.py:197-200 只驗 reset_online_lr 能把 lr 拉回來，沒驗誰該呼叫它）
- **驗證判詞（節錄）**: 上半站得住、下半站不住,且事實陳述有誇大。\n\n【誇大處】「『zoo 建得起來』沒有任何測試把關」不成立。tests/test_model_loading.py:58-166 已有整節 build_generator/build_surrogate 測試(sigmoid/latent/mirror/mlp、未知名字 raise、sm_train 參數流);tests/test_config.py:50-56 更逐支 yaml assert generator/surrogate 名字在 zoo。真正的洞比宣稱窄:**已註冊但沒被建過**的那幾個(cnn/cnn2/ensemble/batch_latent/multiscale/direct)。不過該洞的核心洞見(註冊 ≠ 建得起來)完全正確。\n\n【測試① 站得住,已實測坐實】唯讀跑遍全 zoo(setDefaultCoordinate 後)：build_surrogate(name=cnn) 與 (name=cnn2) 在 rad 開/關兩種情況全部 TypeError: CNNSurrogate() got an unexpected keyword argument 'rad_response'。根因在 antenna/training.py:456-463 無條件傳 rad_response=/rad_n_basis=
- **修訂後行動**: 拆成「留一、砍一」。\n\n留：zoo 建構冒煙測試,但先修 bug 再加測試,同一 commit。\n  (a) 修 antenna/models/surrogates.py:497 CNNSurrogate 與 :548 ResCNNSurrogate 的簽名——照專案「能刪不要包」,建議加 `rad_response=None, rad_n_basis=16` 兩個被忽略的參數並在 docstring 註明「無 rad 頭,參數僅為對齊 build_surrogate 呼叫慣例」(對齊 surrogates.py:685 EnsembleMLPSurrogate 已寫的同款註解),不要在 build_surrogate 端加 if/inspect 分支(那是加間接層)。\n  (b) 測試加進**既有** tests/test_model_loading.py 第 58 行那節之後(不開新檔),約 8 行：\n      @pytest.mark.parametrize(\"rad\", [False, True])\n      def test_every_zoo_entry_builds(tmp_path, spec, rad): 迴圈 zoo.SURROGATES / zoo.GENERATORS 全名 build 一遍,rad 開關各一輪。\n  預期落地後 GEN 6 個 + SM 4 個 × rad 兩輪全綠。工時 S(~30 min),風險 0,不碰批次線。\n\n砍：SM 訓練健康度測試「epochs=3/batch_size=4 assert lr > min_lr」——實測 lr 三個 epoch 後仍是 0.001,必綠、零鑑別力,寫了等於多一條假保證。若發現② 的 lr 崩塌要補回歸測試,守門點應該是它真正發生的地方與規模(script/sm_reanchor.py 逐 batch step ReduceLROnPlateau、數十 epoch 後撞 1e-6,且該路徑不呼叫 reset_online_lr),那不是 <10 行的核心單元

### [soon|S|toolchain] 生成但從未量測的 input 夾被永久登記為「已測」，污染查重與鏈的鄰域枯竭判定
- **位置**: script/dedust.py:4217-4222 `_all_input_folders()`、4611-4625（chain hist_keys）、4634-4652 `_preload_used()`、4757-4761（check-dup 失敗後 `continue` 但不刪已建的 input 夾）
- **驗證判詞（節錄）**: 【核心機制：全部求證屬實】 ① `_all_input_folders()`（dedust.py:4217-4222）的收錄條件只有「夾名 endswith `_input` ∧ 有 manifest.json」——**與有沒有量測完全無關**。check_dup（4225-4258，歷史迴圈 4247-4251）、chain hist_keys（4611-4625）、`_preload_used`（4634-4652）、以及另外 8 個 select/selfgen 的 `hist` 去重集（2289/2570/2755/3029/3068/5363）與 root 索引（3064-3068）全部吃這一個集合。analyze.py:1110-1124 的覆蓋/多樣性段是各自掃（同語意）。→「已生成＝已測」的欄位契約問題成立。 ② check-dup 失敗只 `continue`（4757-4761），已建的 input 夾與 25 個 .pt 永久留在 NAS。commit b891250（2026-07-25 hist_keys 修）只治「少撞」，沒治「撞了留垃圾」。  【NAS 實測（唯讀掃描，數字要修正）】 - 611 個帶 manifest 的 `*_input`，26,236 筆 manifest entry。同名 store 無 results.json 者 = 30 
- **修訂後行動**: 【A. code（S，~5 分鐘）】dedust.py:4759-4761，`cd.returncode != 0` 分支 `continue` 之前補一行 `shutil.rmtree(str(ind), ignore_errors=True)`（檔頭已有其他 import shutil 的先例，就地 import 即可）。**不要**再往下做「pending set／已生成 vs 已量測雙欄位」那類重構——殘留只剩 in-process 的 `used`／`hist_keys`（4726/4747 在 check-dup 前就寫入），生命週期止於該 daemon，不落地、不污染跨 session，為它加狀態機是 over-design。commit 後**不強制重啟在飛的鏈**，隨下次開鏈生效；照 scratch 2026-07-22 SOP 在 ONGOING 記一行「c47d2/後續鏈重啟後才帶新版」。  【B. 清孤兒（S，~15 分鐘，⚠ 刪除條件必須改）】原建議「逐一確認沒有別名 store 引用」太模糊且已被實測打臉。改用可機器驗證的三條 AND：①`dataset/<store>/` 不存在 ②`jobs_state/<store>.claim` 與 `.done` 皆不存在 ③`<store>` 不在 jobs.json（551 筆 append-only 派工史）。這三條同時成立才等於「從未派工」。此predicate 會自動保住 `dedust_c47d2_p03`（在飛）。 額外**白名單排除** `dedust_champ_input`／`dedust_verify_input`：實測其 id 已在 dedust_champ_disc／ref2／ref2v／verify_disc2／verify_interp 量測過，只是 store 改名 —— 它們是真歷史，刪掉會讓已花 HFSS 的 17 筆變成可重生成的浪費（提出者只說「被 HISTORY_INPUTS 引用先確認」，理由不對但結論對）。 執行前先 dry-run 印清單給 Ricky

### [later|S|core] zoo 註冊的 cnn / cnn2 兩顆 SM 走不了 config 驅動路徑——build_surrogate 硬塞 rad_response/rad_n_basis，兩個工廠不收 → TypeError
- **位置**: antenna/training.py:456-464（build_surrogate 無條件傳 `rad_response=`、`rad_n_basis=`）vs antenna/models/surrogates.py:497-509（CNNSurrogate）、548-559（ResCNNSurrogate）的簽名；契約寫在 antenna/zoo.py 檔頭「SM 建構簽名 (checkpoint, in_dim, response_shape, lr, min_loss, max_epoch, **架構參數)」
- **驗證判詞（節錄）**: 【事實：確認】程式碼逐行核對成立。antenna/training.py:455-463 build_surrogate 無條件把 `rad_response=`（rad 關時為 None）與 `rad_n_basis=` 當關鍵字傳出；antenna/models/surrogates.py:497-498 CNNSurrogate 與 548-549 ResCNNSurrogate 的簽名只有 (checkpoint, in_dim, response_shape, [channels/fc], lr, min_loss, max_epoch)，兩個都不收 → 走 config 路徑選 cnn/cnn2 必 TypeError，且與 radiation.enable 無關。mlp(436)/ensemble(688) 收，故只有這兩顆能走 config。反過來看契約歸屬也對 reviewer 有利：zoo.py:14-15 登記的 SM 簽名是「(checkpoint, in_dim, response_shape, lr, min_loss, max_epoch, **架構參數)」，rad_* 不在契約內 → 越線的是 build_surrogate，不是兩個 CNN 工廠。tests/test_config.py:56 只斷言「名字有註冊」、tests/test_model
- **修訂後行動**: 照 reviewer 方向修 build_surrogate（不要改 CNN 工廠簽名），但把兩點寫死： ① antenna/training.py:455-463 改成「rad 關 → 完全不傳 rad_* 兩個 kwarg；rad 開 → 只對支援的骨幹傳，不支援者 raise ValueError 明講『surrogate cnn2 無方向圖頭，radiation.enable 請改用 mlp/ensemble』」。⚠ 千萬別走「在 CNNSurrogate 簽名加 rad_response 然後忽略」那條路——那會讓 rad-enabled config 選 cnn 時靜默產出無 rad 頭的 SM，變成安靜跑錯的實驗，比現在的 TypeError 更糟。 ② tests/ 冒煙測試值得加（zoo 註冊表 × 建構器脫節的長期護欄，之後若真的登記 GNN 骨幹會馬上回本），但要控成本：ensemble 用 ensemble_size=2 + 小 hidden、cnn/cnn2 用小 fc，別讓 CI 每次建 5×4M 參數的 MLP。 ③ 報告/commit 訊息別寫成「解鎖影子轉正」——誠實描述＝「補上 config 路徑對 cnn/cnn2 的可建構性與明確錯誤訊息」；真要讓 cnn2 上 rad 主線，另案評估 ResCNNNet 加 rad head。 ④ 時機：搭在下一次有人動 training.py/zoo.py（例如登記 GNN 骨幹）時順手做，不必為它單開一次 commit-cycle。

## 駁回（1）

- [core] utils.py / losses.py 把 matplotlib＋SMTP 吸進核心 import 路徑，而拉進它們的兩個符號本身是死碼
  - 駁回理由（節錄）: 提案分兩半：死碼事實半正確，效能動機半站不住，而效能正是它被排到「值得做」的理由。  【事實查核 — 成立】 1. antenna/utils/utils.py:341-356 plot() 確為死碼（全 repo 零呼叫；只有 __init__.py:22 re-export；archive/*.py 星號匯入但從未呼叫，且 archive 不在 CI pyflakes 範圍 antenna/train.py/KuoHung.py/tests/script 內）。 2. utils.py:69-94 errorCallback() 確為死碼（僅 __init__.py:18 re-export）。 3. 提案漏講的一項：__init__.py:30 的 Axes re-export 同樣零消費者——losses.py:37 / pattern.py:17 / response.py:20 / optim/scheduler.py:15 全部從 antenna/utils/types.py:17 取。 4. utils.py:47 ReturnType TypeVar + :497-5

## 未驗證備查（21,信心排序）

- [tests|S|conf0.90] **_chain_score 零測試覆蓋，−99 哨兵語義過載；整包被 gate 掉時鏈帳仍記一個假的 best**
  - tests/ 全域搜不到 _chain_score，但它是全系統單筆成本最高的判斷：決定一條鏈（20 包 × 25 筆 HFSS）往哪爬。三個問題疊在一起：(a) −99 同時代表「掉出盆地」與「這筆沒有方向圖資料」——`r = -9.0 if rad_margin is None` 會讓缺 rad 的樣本一律 gate 成 −99，整包 rad 萃取失敗會被記成「軸枯竭 dry」而不是「儀器壞了」；(b) 全部 −99 時 `max()` 回傳迭代序第一筆，`rec` 照樣寫 best/wm/rad/lo/hi——c47d1.jsonl 兩包都是 best_score −99.0 卻記著 `best: c47d1p01_00, wm −6.94`，讀帳的人（或未來的 agent）會把「樣本
  - 行動: ① 補參數化回歸測試：五個 goal（wm/dual/tri/lo/hi）× {合格、掉出門檻、缺 rad_margin、缺 oob_gain_max_lo} 的期望分數，把 −99 哨兵與各 gate 釘死（純函式、零 NAS，S）；② rec 加 `gated` 欄（−99 筆數），best_s <= −98 時 best 記 None、不寫 wm/rad/lo/hi、win 強制 False（約 4 行）。
- [tests|S|conf0.90] **`r.get("rad_margin") or -9` 會把 rad_margin 恰為 0.00 的合格解判成不合格——錨點池漏掉貼線三標解**
  - Python 的 `or` 把 0.0 當假值，所以 rad_margin 剛好 0.00 時取到 −9 → `tri` 判 False。而 rad_margin 是 `_r()` 四捨五入到小數 2 位（dedust.py:648）的量，落在 0.01 網格上，0.00 完全可達；decisions 的硬閘門定義又正是 `rad ≥ 0`，也就是說被吃掉的恰好是「剛好過線」的合格解。這三處都在 select-* 自動吸收前批三標贏家、建 ANCH 錨點池的路徑上，漏掉的解等於永遠不會被拿來當錨開採。同一行的 `(oob_bad or 99)` 同型（實務上 oob_bad=0.0 罕見，順手一起修）。analyze.py:817 已用 `if ... is not None else -9
  - 行動: 三處改成 `(r["rad_margin"] if r.get("rad_margin") is not None else -9)`（或抽一個三行小 helper，不要包成類別），順手統一 analyze.py:825 的同型寫法；照 tests/CLAUDE.md「每修一個 bug 補一條回歸測試」補一條：rad_margin=0.0 的 entry 必須被判為三標過。
- [core|M|conf0.85] **SM 訓練的 ReduceLROnPlateau 以「每個 batch」為單位遞減 → lr 在第 1~2 個 epoch 就撞 1e-6 地板，之後整個訓練預算在空轉（主 KPI 的儀器被廢掉）**
  - 決策層 2026-07-15 把「SM 準度」定為主 KPI，目標線＝近域 <0.8 / 遠域 <1.5 dB，但 docs/kpi.csv 從 v82 到 v86 卡在 frozen_med 1.10~1.38、frozen_far 2.5~3.0，十幾版沒有斜率。實測 NAS 上 6/6 個 checkpoint（sm_reanchor84/85/86、sm_two85、sm_shadow85、sm_ens86_1/2）的 optimizer lr 全部＝1e-6＝min_lr 地板；scheduler state 的 last_epoch=94,400＝40 epoch × 2,360 batch，證實 scheduler.step() 是每個 batch 呼叫一次（patience=
  - 行動: ①把 scheduler 從 batch 迴圈拆出：surrogates.py:144 改成迴圈內只 `self.optimizer.step(None)`，在算完 avg_epoch_loss 之後（約 line 150）補一行 `if self.scheduler: self.scheduler.step(avg_epoch_loss)`——patience=10 的單位回到 epoch。②在 script/sm_reanchor.py 每個 `pre_load_model(...)` 後補
- [toolchain|S|conf0.85] **chain 收檔缺「整包全部出局」偵測，全 −99 的包被當成正常一包記帳**
  - tri/lo/hi/rad 四個 goal 都是「門檻外→−99」，所以錨落在盆地外時整包 25 筆同分 −99，`max()` 挑到的是任意一筆，jsonl 只記 `best_score: -99.0`、沒有任何欄位或告警說「這一包沒有一筆進入目標盆地」。c47d1 就是這樣燒掉 50 筆才由人在 round 檔手寫「廢鏈=擇錨口徑事故」。今天已記帳的坑①治的是發車前驗錨，這是收檔端的互補缺口，而且也涵蓋「鏈爬著爬著離開盆地」的情形（發車前驗錨管不到）。附帶風險：`--anchor-score` 不給時 `win = True` 恆成立（CLI help 明寫「不給=首包必換錨」），此時錨會被換成一筆 −99 的 pattern。
  - 行動: chain 在 4783 之後加三行：若 `best_s <= -99`（或所有 scored 皆 −99），jsonl 記 `out_of_basin: true`、印出錨的 goal 鍵實測值、直接收鏈不再發下一包。
- [methodology|S|conf0.85] **主 KPI（SM 準度）用會漂的尺判定；凍結尺顯示自 07-16 起沒有斜率**
  - 專案自己定「輪的成功=KPI① 有斜率」，且 2026-07-16 就因「held-out 隨批擴張=尺在漂」立了凍結基準集。但收輪報的仍是 rolling 欄。實測對帳：①rolling wm_err_med 全史最低是 v39 1.169 / v37 1.184 / v35 1.192（07-15~16，heldout_n 1595-1772），v85 的 1.227「新低」在全帳上不成立，只是近期低（rolling 集已擴到 6105 筆，本來就不可跨版比）；②凍結尺 frozen_med 最佳仍是 v41 1.012（07-16），v86 1.097 沒破；frozen_far 最佳 v40 2.218，現在 2.776=更差；③主產通道 two 的 two_frozen 25 版（
  - 行動: ①收輪 §4 的 KPI① 一律改貼凍結欄（kpi.csv frozen_med/frozen_far＋kpi_two.csv two_frozen），rolling 只當輔助且標「不可跨版比」；②在 round-46 §4 補一條「★ 修正」註記 rolling『新低』的口徑限制（append-only 規則）；③高原條件③ 重寫成一行可算式，例如「two_frozen 連 5 版最佳值不破且 slope>−0.005/版」——照此現況即為成立，該結果直接回報 Ricky；④凍結集加一個深水子
- [methodology|S|conf0.85] **高原三條件目前不可判：①靠未校準的 dry2（實測假停率 76%）、②沒有定義、③尺未指定**
  - 三條件是要用來下「研究是否進入真高原」這種戰略級判斷的，但三條都還不是可計算的判準。①「接力線 dry 收鏈且終點進不了作戰區」——終點是 dry2 這個停止規則的輸出。用鏈帳做假停測試（前鏈最後兩包皆 win=false，後鏈從同錨重開）：17 對接棒鏈中 13 對（76%）仍有正增益，中位 +0.04、最大 +0.34（c6tri4→c6tri5），g 線本身就有一例（c45g2 判 dry → c45g3 再 +0.12）。好消息是偏誤量級小（≤0.34 dB），撐不起 1.7 dB 的缺口，所以條件① 的結論站得住——但「終點」必須報成「終點＋殘餘偏誤 ~0.04(中位)/0.34(最大)」才誠實。②「下一個新機制首輪零增益」完全沒有操作化：什麼算「新機制」、「首輪」幾筆、「零增益」
  - 行動: 把三條件各改寫成一行可計算式並寫進 scratch/decisions：①「接力鏈 dry3 收鏈 ∧ 終點 wm<−1 ∧ 接棒鏈再 2 包增益 <+0.1」；②「新機制＝首次進批/鏈線的算子或模型，首輪 ≥60 筆，主指標（該機制宣稱推進的那一軸）Δ<其歷史批間 sd」——發車前指定是哪一軸；③用 two_frozen 的連版判準（見發現 1）。另外把「17 對接棒鏈的假停統計」寫進 round-46 §4 或 scratch 當成 dry2 的校準常數，之後所有鏈終點都附這個誤差棒。
- [knowledge|S|conf0.85] **/batch-cycle ③ 重錨清單缺 `train-two`——select 端靜默降級回退役 MLP 通道**
  - skill 文本停在 2026-07-13，只寫 `sm_reanchor train --add`。實測 `train` 會自動配訓 rad 頭與影子 CNN（--no-rad/--no-shadow 才關），但 **train-two 是獨立子命令、不會被 train 帶出來**；而它產出的 `sm_twoNN.pth`／`sm_loheadNN.pth` 正是 R38b3 起 O 臂排序實權與 R40「two 換裝」後的絕對值通道。照 skill 走的 session 會產出缺 two 的版號，select 只是 `if exists()` 判斷、沒有 else／沒有 manifest 欄位 → 整批選樣**靜默**退回退役 MLP，事後從 manifest 也分不出來。這比已知的 G
  - 行動: skill ③ 改成兩行（`train --add … --out sm_reanchor<NN+1>.pth` ＋ `train-two --out 同版`），夾數改寫成「照該 round §3」；dedust.py 三處 exists 判斷補一行 else 警示或寫進 manifest 一個 `sm_two` 欄位（治本，讓降級留痕）。
- [knowledge|S|conf0.85] **ONGOING.md 的「🔵 進行中/待跑」區已失能：190 行是已歸檔 round，且同一結論散文複製於三處**
  - 這是 /takeover 第一步要讀的 live 操作板，真正 live 的只有頂端戰況塊＋R47 一行＋資料工廠塊，其餘全是舊 round 的完整結論散文——同一結論在 round 檔 §5、docs/log/README.md 索引行、ONGOING 條目三處各存一份（指向不複製的具體漂移實例：R28「塊內手術修不回 rad／承重圖」在三處各有一版措辭），任一處修正都不會傳播。標頭日期落後內容六天，讓接手者無法用它判斷新鮮度。噪音成本每個 session 都要付一次。
  - 行動: 🔵 區只留 R47＋資料工廠兩塊，其餘條目刪除（log/README 已有索引行，刪不是搬——能刪不要包），確有價值的候選搬 🔜 區；標頭日期改成收輪時順手更新（併進 /close-round 清單既有的 ONGOING 那一步）。
- [tests|M|conf0.85] **公證（3/3 重測）資料實際上從未進入 SM 訓練集——去重「certified 先見先贏」的不變式已失效**
  - _load_clean() 以 pattern bytes 首見即贏去重，而檔案順序就是優先序（檔頭註解明寫「certified 店排最前，以公證店的正確響應為準」）。但 (a) `--add` 只 append 檔尾：現存 8 個公證店（r20n/r22n1/r22n2o,h,w/r22n3/r23n1w,r23n1r）全部排在其對應批次店之後（clean_stores.txt:70-113 可直接看到 r22b1a-f 緊接 r22n1），公證重測的是同一個 pattern → key 相同 → 全數被原始單次量測蓋掉、對訓練零貢獻；(b) R23n2a 之後 NAS 上還有 r24n1a/r24n2a,b/r25n1a/r26n1a/r28n1a,b/r29n1a/r36n1/r38n
  - 行動: ① _load_clean_stores() 讀完清單後做一次穩定前移：檔名符合公證命名（r\d+n）者移到最前（約 3 行，不加抽象層）；② 把漏掉的公證店補進 clean_stores.txt；③ 補回歸測試：monkeypatch DATASET_PATH，造兩個含同一 pattern、不同 response 的暫存 store，斷言 _load_clean() 取到公證店的值（照 test_jobs_add_concurrent_lock 的手法，零 NAS）；④ 修完重錨一版，比對 k
- [research|M|conf0.85] **標量 rad 判別器化石化：rad 是全系統綁束軸,卻是唯一沒進制度合訓、也沒進記錄鍵的頭**
  - rad 是現在每一條血統的綁束軸（左側家族 rad_margin +0.03~0.07 全靠弱側貼線、t09 系只卡 rad、c45g 終錨 rad −0.28）,而測 rad 的儀器是全場最弱的一支;同配方的 lohead ρ 0.76–0.83 已證「標量判別器比 51 維曲線好學」的假設成立,rad 沒理由不比照。更關鍵:scratch 2026-07-24「t07 R39 候選」的觸發條件字面上就掛在「radhead2 上線後在 t07 鄰域讀到 rad 梯度」——等於一整條研究分支被一個沒接完的線頭卡住五天。
  - 行動: ①把 train_radhead2 併進 sm_reanchor train 的每版合訓（照 lohead 既有寫法,不新增抽象層）②kpi_two.csv 加 radhead_rho/radhead_mae 兩欄=rad 準度曲線 ③select 只記 pred_rad2 供前瞻對決舊 rad 頭,先不進鍵（照考試制:離線三尺→實戰盲測連兩批→轉正）。
- [toolchain|S|conf0.80] **check-dup 的 exit code 把「有重複」和「腳本崩了」混為一談，chain 又把 stderr 吞掉**
  - check_dup 會因為 manifest.json 不存在、JSON 壞檔（jobs.json 已有兩起並發壞檔前例）、NAS 短暫斷線、torch.load 失敗而丟 traceback，退出碼同樣是 1。chain 收到後一律解讀成「撞歷史」，pack += 1 繼續下一包，stderr 被 capture 掉沒人看得到。最壞情況是整條鏈把 max_packs=20 個包號燒完、零筆 HFSS、每包留下一個孤兒 input 夾（與上一條複利），而帳面上看起來像「這個錨的鄰域枯竭了」——一個錯誤的科學結論。
  - 行動: check_dup 有重複時改 `raise SystemExit(2)`，讓 exit 1 專屬於「崩了」；chain 只在 returncode==2 時視為撞歷史，其餘印出 `cd.stdout[-500:]` 與 `cd.stderr[-500:]` 並中止鏈。
- [methodology|M|conf0.80] **盆地肥沃度早篩：5-8 筆探針在深水錨幾乎沒有檢定力，且既有 dry2 已把死盆地成本壓到 ~50 筆**
  - 用鏈帳反推每樣本改善率：win 的定義就是「25 筆中至少 1 筆勝錨」，故 p=1−(1−pack_win)^(1/25)。實測 pack 勝率 深(≤−2) 0.61 / 中 0.53 / 近 0.38 → p≈0.037 / 0.030 / 0.019。6 筆探針的 P(≥1 勝)=0.20 / 0.17 / 0.11，8 筆=0.26 / 0.22 / 0.14——肥沃與貧瘠的對比只有 0.20 vs 0.11，等於沒有解析度；要分開需 n≈100+。提案裡引為佐證的「x00 存活 72% vs 左側刀鋒 0/16」是另一個統計量（合格錨的鄰居存活率），那個確實 6 筆就夠力（P(0 存活|72%)=0.0006），但它只在「錨已合格」時有定義，而接力鏈的錨全在 wm −8 的深水，
  - 行動: 不新增探針階段（能刪不要包）：①落實 scratch 已提的 chain 啟動驗錨——用 _chain_score 同源欄位算錨分，盆地外直接 refuse 發車（c47d1 事故的真解，S）；②把「肥沃度」直接定義成「pack-1 的 25 筆是否勝錨」，即現行第一包就是最小可用探針，需要更保守就對深水鏈把 --dry 調 3；③早篩若要做，限定用在合格/近合格錨上並用存活率統計（power 足），寫死「6 筆全不合格→棄錨」；④校準一律走既有 140 包鏈帳（零 HFSS），不要另開量測。
- [methodology|M|conf0.80] **c47d2 單鏈答不了「起點品質 vs 血統天花板」：處理未維持、多因子混雜、對照是單一歷史鏈**
  - 三個獨立問題。①處理不持久：實驗操作是「起點 rad≈0（+0.44）」，但鏈帳顯示 p01 rad −1.93、p02 −2.21，兩包內處理就被消耗掉了；g 線也一樣（起點 −0.94 → 中段 −1.9 → 末段 −0.28），因為 tri 鍵在 wm−0.15 遠低於 rad 時對 rad 完全不施壓。等 c47d2 真的爬到 −2.7 帶（若能），它早已不是「rad 均衡血統」，因果通道不存在。②多因子：c47d2 vs c45g1 除了 rad，還差生成臂（G free randf denovo vs free_rand）、lo 深度、驅動變異的 SM 版本（v86 vs v80）、批次；而且兩個錨的 lo 是不同欄位口徑（R45 的 −6.18 對 R47 的 gain_max
  - 行動: ①先做零 HFSS 的替代分析：140 個包已含（錨分, 錨 wm/rad/lo, 本包是否勝錨, Δ）——直接回歸/分層看「控制 wm 深度後，錨 rad 是否預測下一包 Δwm 與勝錨率」，這是同一個問題的高得多樣本量版本，一小時內可得；②c47d2 不停（資料照收），但把 §1 判準的措辭事前改成案例級（「破 −2.7=與假說 A 相容的第一個案例」而非「假說 A 成立」），並註記處理不持久這個限制；③若之後仍要用鏈做因果，改配對設計：同一批出的兩顆錨、wm/lo（同一欄位口徑）匹配、只差
- [knowledge|M|conf0.80] **鏈線（tier 0）在 skill 家族裡零覆蓋——主要變現線沒有 runbook，c47d1 廢鏈是可預防事故**
  - 2026-07-21 tier 架構定案後，鏈線是實際產出主力（usable_lo −3.46 紀錄、R45/R46 全部推進都出自鏈；R47 §3 半數指令是鏈），但 /batch-cycle 主 runbook 一字未提鏈、/takeover 的定位表也沒有「鏈跑到第幾包」這一列。結果是擇錨口徑、發鏈參數、逐包判讀、dry2 收鏈、jsonl 記帳全靠當班 session 的記憶——2026-07-29 c47d1 就因為擇錨誤用 `contrast_lo`（tri 鍵實際門檻是 `oob_gain_max_lo`）整鏈 −99，燒掉 50 筆 HFSS。scratch 已記事故並提「工具面驗錨」修法，但**流程文本層沒人補**；鏈線是現在唯一在爬的線，重犯成本是半天機時。
  - 行動: 在 batch-cycle SKILL.md 末尾加一節「⑧ 鏈線」約 5 行即可（擇錨欄位一律 `oob_gain_max_lo`／發鏈指令模板／逐包看 jsonl／dry2＝換錨或收線／記帳落 round §3 表），並在 takeover 定位表加一列「鏈在第幾包」。不另開新 skill（能扁平不巢狀）。
- [knowledge|S|conf0.80] **寫作交接包 §5 的地盤邊界已被實際越界，§0 也沒指向寫作 session 已產出的 methodology 兩檔**
  - §5 寫死「寫作 session 地盤＝docs/report/ 新 stem，只新增不改既有檔；script/ 由批次 session 獨佔」，但實際產出落在 docs/ 根、動了 script/figs/ 與共用的 CLAUDE.md／memory——併行規範在第一次使用就失效，而真正需要的規則（共用檔 CLAUDE.md／decisions／memory 怎麼避免對撞）反而沒寫。同時 §0 閱讀順序與 §2 真相源表都沒有 methodology.md／divergences，下一個寫作 session 照包接手不會知道已有 725 行方法論底稿與 29 條論述分歧判斷，極可能重造；§4「未定案清單」也已與 divergences 台帳形成兩份會分岔的清單（違反指向不複製）。
  - 行動: handoff §0 加兩行指向 docs/methodology.md 與 methodology-divergences.md；§4 改成一句指向台帳二節（刪本地清單）；§5 邊界改成實測版並補一條共用檔規則（CLAUDE.md／decisions／memory 一次一人、改完即在對話說）。
- [tests|M|conf0.80] **golden 安全網保護的是 antenna/（9.5k 行），但研究決策已整個搬到 script/（≈9.1k 行）且幾乎無網——下一次結構性重構的風險落在沒防護的那一半**
  - CLAUDE.md 把 golden 定位成「任何結構性改動的重構安全網」，而它確實把 antenna/ 核心守得很好（2026-07-01 稽核也證實了）。但兩年下來研究產出的真正決策層——選批鍵、錨點池建構、判讀彙總、SM 訓練集組裝——全都長在 script/ 這 9.1k 行裡，且測試只到「純幾何函式」為止（strip_small、oob_metrics、_group_mutate…），沒有一條測試碰到「一份 results.json 進去、判讀數字出來」這條路徑。北極星「核心精簡」最想拆的恰恰是 6894 行的 dedust.py，而現在拆它沒有任何回歸訊號：改壞一個 tri gate 或一個排序鍵，pytest 照樣全綠，錯誤只會以「這輪三標率怎麼低了」的形式在幾天後、混在 HF
  - 行動: 不加抽象層、不建框架——凍一份最小 fixture：tests/fixtures/ 放一個 ~10 筆的 mini results.json ＋ 對應 manifest.json（涵蓋合格/缺 rad/貼線/error 四型），再補一組釘住彙總數字的測試：_chain_score 各 goal、sel_score、三標計數、analyze 的批次彙總行。一次性 fixture＋斷言，之後每次改決策層都有訊號。
- [research|M|conf0.80] **d=1 有限差分語料未做結構盤點：3,519 筆已量測的鄰域粗糙度＋勝步算子/座標分布**
  - 三個直接用途:①「刀鋒 vs 平緩」第一次有全帳數字,深水系粗糙度只有左側系的 1/4——這是 −2.7 牆「爬不動」的量化候選機理（不是算法不行,是該區局部平坦、d=1 沒有可用梯度）②勝步的像素座標可疊成空間先驗,直接餵組文法生成端的位置軸（decisions「組文法生成系統」列的「質心熱圖離散→位置軸必須加溫」缺的就是這份資料）③論文地形/算子章目前沒有 d=1 尺度的證據。與已知候選「盆地肥沃度校準」共用同一批數字但用途不同:那條是「開鏈前探針的決策規則」,這條是「已有語料的結構全帳」——報告時要講清楚 delta,別混報。
  - 行動: 一支 scratchpad 腳本 join manifest+results,出三張表/圖:分血統粗糙度分布、勝步算子分布（含 grp vs px 的配對勝率）、勝步像素熱圖（疊在錨 pattern 上）;結論寫成 analysis-07,decisions 不動。
- [research|M|conf0.72] **同 HFSS 預算對比曲線缺席：鏈線 vs random best-of-N 從未同尺畫過（可能翻案「輸 random」定論）**
  - 全專案最強的量化主張=「工具箱調度優於單一優化器」,但跨線同尺圖只到 R14,現在的旗艦成果（鏈制接力）完全沒有預算對照。粗算已可看出翻案潛力:g 系四段接力 23 包=575 次 HFSS 把 wm −8.04→−2.72,而 R6 期望式 −9.18+0.75·ln(575)=−4.41 → 領先 ~1.7 dB;memory project_benchmark_vs_random 記的定論是「學習式搜尋輸 random best-of-N」。這是零 HFSS 就能做、且同時服務 8 月文件與定論修訂的唯一一張圖。
  - 行動: 在 expected_best report 加一條「鏈軌跡」序列（x=累計 25×pack,y=best-so-far wm）,把 g/d 系（深水）與 tri 系（左側）疊到既有 random best-of-N 曲線上;誠實標兩個 caveat——①起錨那批的 HFSS 成本要計進 x 軸 ②池是「偏好區」分布=上界參照非 uniform。結論落 analysis-07,不改 thesis_outline 數字前先給 Ricky 過帳。
- [tests|S|conf0.70] **check-dup 的「查到重複」與「自己崩了」共用 exit 1，chain daemon 會把 NAS 讀檔失敗誤讀成撞歷史、無聲空轉燒完 max_packs**
  - check_dup 的 load_folder 對歷史夾沒有任何 try/except（對比 chain:4617 與 _preload_used:4650 都有），任何一個歷史 manifest 壞檔或 .pt 讀取例外都會讓它以未捕捉例外結束——Python 未捕捉例外的 exit code 也是 1，與「查到重複」完全無法區分。chain 收到非 0 就印「撞歷史——重抽下一包（不發車）」並 continue，但 pack 已經 +1、dry 不加：結果是每一包都被判撞歷史，鏈把 max_packs 燒完、零 HFSS 發車，然後安靜地印一行收鏈退出。這些 daemon 是無人看顧跑數天的（jobs.json 已有兩起壞檔前例，scratch 2026-07-22/07-24），且失敗
  - 行動: ① chain 端兩行判別：`cd.returncode != 0` 時，只有 stdout 含摘要行（「重複」）才當撞歷史 continue，否則 raise SystemExit 停鏈報錯（保留 CLAUDE.md「exit 1 就別發車」的對外約定不變）；② 補兩條回歸測試（monkeypatch DATASET_PATH，同 test_jobs_add_concurrent_lock 手法）：kind=notarize/repeat 的重複條目不算違規可通過、批內與跨夾重複各觸發 exi
- [research|M|conf0.70] **鏈帳反事實重播：3,519 筆真值上離線比「SM 排序挑」vs「隨機挑」,零 HFSS 驗證論文最強翻案**
  - decisions 明寫「SM 引導在某預算後勝過 random=論文最強潛在翻案」,但至今唯一證據是 --expert 兩包（n=2,best 口徑 2/2）。鏈是 d=1 尺度,而現有 SM 排序力只在批線寬範圍量（前瞻 ρ）——用途與量測尺不對齊。重播可一次得到 141 個配對包、每包 25 個真值的排序 regret,既回答論文主張,也回答營運問題「純隨機包還要不要燒」（若 SM top-3 顯著贏隨機,鏈就該全面上 expert 排序;若不贏,--expert 標配該撤）。
  - 行動: 寫一支 scratchpad 腳本:對每包載入 25 個 .pt,用「該包收檔日期之前最新的 sm_reanchorNN」打分（防洩漏）,計算 top1/top3 選中者的 _chain_score vs 該包隨機挑的期望與實際勝錨結果;輸出勝率、平均 regret、按血統/goal 分層。先跑 3 條代表鏈驗證管線,再全跑。
- [research|M|conf0.68] **SM 資料規模報酬曲線從未量過（固定架構子抽樣消融）——高原辯論缺的便宜先驗**
  - 「真高原三條件」的②「下一個新機制首輪零增益」要燒 HFSS 才測得到,而「邊際資料價值」是零 HFSS 就能量的先驗:曲線仍陡→續燒資料是對的、押鏈線合理;曲線已平→瓶頸在表示/架構,GNN bakeoff 之類該優先。目前所有「資料飛輪在轉」的判斷都靠 kpi.csv 的版本時序,而該時序同時混了資料增量、反權重改版、分布漂移三個變因,不能當因果讀。這條曲線同時是 8 月文件裡「資料飛輪」一節唯一能放的量化圖。
  - 行動: 固定 v86 配方與 seed,用 10/25/50/100% 訓練子集各訓一顆,同一把凍結 held-out 量誤差,畫 err vs N（log x）;順手加一條「關掉密度反權重離散化」的對照線,驗 scratch 07-28 對 v85 跳檔的嫌疑。全程開發機背景跑,不碰生產線與 NAS 寫入。
