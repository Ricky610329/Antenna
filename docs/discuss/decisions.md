# 確定結論／決策（decisions · 確定層）

> 兩層「討論記憶」的**確定層**：經確認的結論、定案、方向。Claude 新增條目時**會主動告知你**。
> 半熟點子在 [scratch.md](scratch.md)（隨意層，Claude 不主動報）。研究實驗的正式記錄仍走 `docs/log/`——
> 本檔只放「討論中定案」的精簡條目（指向，不複製 docs/log 的內容）。新的記在最上面（新→舊）。

## ★★ 兩條量測誠信鐵則（補記 2026-07-28;規則本身自 2026-07-07 起就在跑,只是一直沒有 decisions 條目）

> 補記理由：這兩條是本專案**最核心的方法論紀律**,但正文一直只存在於 `docs/log/CLAUDE.md`
> 「三條硬紀律」,decisions 只有戰績引用。交接/寫論文時容易漏掉法源——故補條目。**規則不變,只是登錄**。

- **鐵則一・判準發車前寫死**：round §1 的判準必須在**發車前**填完（`/new-round` §1 六條檢查表,缺一不發車）。
  發車後要修訂：**只能在任何結果回來之前**,且必須留修訂註記（日期＋理由＋「發生在結果前」聲明）。
  範例＝round-17 §1 的帶外紀錄 9.04 修訂。**why**：事後改判準＝把探索性分析包裝成確認性結論。
  搭配 append-only（收檔後不回改結論,用「★ 修正（日期）」原地補註,範例＝round-10 的 w17 +0.48）。
- **鐵則二・紀錄級宣稱一律公證 3/3**：任何換王／破紀錄／榜首宣稱,都要把該設計用**相同配方**送 HFSS
  **重測兩次**,「原測＋兩次重測三次一致（挑戰指標差 ≤0.03 dB）」才認證。未公證數字一律標「單次」、
  不進結論粗體。**why**：已攔下四個假象——w17 +0.48→−0.06、b20 +0.32→−0.19、
  o26b3_022 +0.53→+0.44、h6_010 +0.43→+0.35。物理前提＝生成決定性＋雜訊地板 ≈0（跨機 bit 級一致）。
- **法源與落地**：正文 `docs/log/CLAUDE.md`「三條硬紀律」;流程 `.claude/skills/new-round`／`notarize`;
  機器真相源 `docs/records.json`（換王記帳順序：records.json → 對比圖 → champions.md → round §4 → memory）。
  ⚠ 現存唯一未公證的紀錄軸基線＝`usable_hi −5.92`（records.json 已誠實標「全史掃描（單次量測）」）。

## ★ 反自餵雙軸（Ricky 2026-07-14:「資料集跟模型的自我正向強化」）
- **問題**：SM 用王系資料訓→選出像王系→資料更王系。兩層:①血統面——根稅只治根標籤,治不了
  「掛別根但長得像王」（冷支 g16/a024 系同屬 w17 實心語言）;②響應面（更深）——**全語料 7,290 筆
  低側對比 >2dB 僅 1.2%、中位 −0.06**:SM 沒看過低側會動的樣本→學不會→選不出→資料永遠低側死平
  （oob 排序半死+前瞻蹺蹺板的資料面根因）。
- **機制①相似度稅**：`--dyn-simcap`（d_dyn=對全王朝系錨最小 Hamming,<12 的近王樣本批內佔比 ≤cap;
  R28b2 起生效——Ricky 同日兩次加壓:0.35→0.20→**0.12,且相似半徑 d<12→d<20**〔23:1x〕,select-r28 預設）——與根稅同語法,治血統面盲區。
- **機制②低側資訊配額**：lo-active 家族臂（Y/D/F/池頂錨）配額為「響應面多樣性」主載體
  （R28=39%）;**R29 起推 ~50%**。低側變異=第二資訊軸,收集端優先補——pred 篩選不可靠
  （SM 低側沒學過=雞生蛋）,結構代理（家族配額）為準。
- 評估照「探索型介入評估法」:不做單批判決,跨輪看 oob 前瞻 ρ 與低側對比 >2dB 佔比的長曲線。

## 每輪硬上限 3 批（2026-07-13,Ricky:「R24 不要太厚,限制每輪內部就 3 批」）
- **規則**：一個 round 內部**最多 3 批**;第 3 批收檔判讀後即 /close-round → /new-round,不拖第 4 批。
- **why**：R23 拖到 4 批＝過厚（§4 難讀、假設早被回答還在灌）;輪＝一個假設的生命週期,不是資料桶。
- **不衝突處**：擴散型介入的長 baseline 評估**跨輪累積**（gain-check L2 比多輪斜率）,不需要單輪撐多批;
  紀錄照公證（事件不是假設）。落地:docs/log/CLAUDE.md 硬規則、new-round §1 判準、batch-cycle 分支表。
- **對 R24**：現在 b3 收檔中 → b3 判讀後收輪 → R25。

## ★ 探索型介入的評估法：效率 over 長 baseline,不做單批因果判決（2026-07-13,Ricky 修正）
- **癥結（Ricky）**：根稅（--root-cap）動的是**錨點抽樣分布**（擴散型介入,把探索攤到更多根系）,
  **不是瞄準帶外的定向槓桿**。期待它「第 N 批打穿帶外牆」＝把擴散介入當定向槓桿的類別錯誤;
  在現樣本量下把帶外紀錄歸因「根稅 vs 隨機」本質做不到。「甚至有沒有這因果關係」＝問得對,可能沒有。
- **正確評估框架**：擴散型探索介入（根稅/de novo/novelty/資訊臂…）**用探索效率評,不用單批命中/紀錄**:
  ①**gain-check L2 學習曲線**（邊際增益 b/N over 累積 HFSS 預算）——介入線 vs baseline 線比斜率;
  ②覆蓋度（根多樣性/新穎樣本佔比,analyze credit/data）;③**≥5 round 後才比**（擴散效應需長 baseline）。
- **鐵律（防過早判決,延伸「防過早悲觀/樂觀」）**：**不對擴散介入下 per-batch 因果判決**;
  各批只累積資料,紀錄照公證但不當「介入成功/失敗」的證據。R24 據此執行:根稅評估=多批後 L2 曲線對比。
- **可正當歸因的反例**：定向介入（如 O 臂 pred_oob 直接選帶外、H 臂劑量掃描）才可做較短視窗的因果判讀;
  擴散 vs 定向的分野=能不能指出「這批的好解是這個介入直接挑出來的」。



## ★ 軸相關枯竭：核心導航原則（2026-07-13,Opus 觀察+Ricky 確認「確保方向不迷失」）
- **原則**：枯竭/打轉是 **目標軸 × 分布區** 的二維現象,**不可用單一指標判全局停滯**。
- **實證（R23b4）**：可用帶外(軸)×王朝鄰域(區) 連四批逼近 9.09 不穿=打轉;但**同一批**
  margin(軸)×王朝(區) 撞 m23b4_030 **+0.49 巨躍**(超現任 +0.08,採 ~600 筆後隨機仍找到新高)。
  ⇒ 不同軸在同一分布區的枯竭速度不同;某軸打轉 ≠ 該區枯竭。
- **對策（已內建 R24 降根）**：降根治「帶外軸打轉」(換分布找新前緣),但**保王朝 margin 產線**
  (保底 25 席+70/30 抽樣)——不因帶外打轉就連根拔還在產 margin 的礦脈。
- **★ 監控觸發器（寫死,防未來 agent 一刀切迷失）**：王朝血系(DYN)子代 best wm **連兩批無新高
  → 王朝 margin 軸也枯竭 → root-cap 0.4→0.6 + 保底 25→15**(此時才全面降王朝)。
  當前(2026-07-13):b4 王朝出 +0.49 新高 = 活躍 → 維持 0.4/25。矩陣是活的,每輪重判。
- **↳ 持久化教訓（同事件）**：此原則首次記錄時因 NAS/持久化事件未落地,重記於此;
  **關鍵操作後照 /reconcile 驗證落地再往上蓋**（見 reconcile skill）。

---

## 可製造性定義＋「歷史資料為主資產」方向（2026-07-03）
- **定案（Ricky）**：lab 真目標＝找出**滿足性能（S11+Gain+rad ±45° 窗內夠高）且可製造**的 pattern。
  可製造＝**裁切製程：允許不連通、但不要很多 1×1 碎片**（不必全連通；1px 孤島是主要禁忌）。
- **方向定案**：整個研究從線上學習走來,但不必然走下去——若「採樣(分布)≫策略」成立（round-06 已實錘
  200-450× 等效預算差）,用歷史紀錄提升是允許的。線上學習框架不丟、改當「warm-start 局部精修器」。
- **研究定位（Ricky 補充定調,2026-07-03 晚）**：研究的主詞=「給定 spec+預算,找出達標且可製造的 pattern」
  的**方法論**;線上學習是前代對它的一個答案、是方法論中的一個階段（局部開採,analysis-01 variogram 給了
  射程依據）,不是研究本身。歷史資料三用途：**起點**（池=全歷史運氣的累積資本,單調變好）/**尺**
  （expected_best 期望帶,取代單 run 運氣對比）/**教材**（結構規則歸納→先驗;analysis-01 統計版、
  SM 歸因候選=模型版）。方法論一句話：池篩起點→結構規則修形→HFSS 驗證→線上學習收尾→結果回灌池。
  對外表述紀律：說「線上學習的正確角色是局部開採」,不說「線上學習沒用」。
- **實作去向**：Round 7 除塵驗證（池達標 pattern 拔粉塵 HFSS 重驗,`script/dedust.py`）；
  數字依據見 scratch 2026-07-03 戰略討論塊（已升級）與 [round-07](../log/round-07-dedust.md)。

## round 編號只給「燒 HFSS」的實驗（2026-07-03）
- **定案**（Ricky）：round 編號保留給**實際使用 HFSS 的實驗**；純離線分析不佔 round。
- **實作**：離線分析改用 `docs/log/analysis-NN-<slug>.md` 系列（首篇 analysis-01 pattern 解剖），
  在 docs/log/README.md 另立索引小節。round-06（離線期望基準）已歸檔、不回改——慣例向後生效。

## 滑動視窗 SM 訓練量（2026-07-03，Round 5 主角）
- **由來（R4 實錘×2）**：① 深度欠訓——每輪訓完 elite 的訓練 loss 仍 7.7–10.6（學長壓到 0.1，差兩個數量級），
  連訓練集都沒擬合；② adaptive 探測自鎖——target 停 3–5、曲線 80–100% 平（快照擠低處→差異<雜訊→沒有往上的證據）。
- **兩端都被自家資料反對**：1–5 ep 欠訓（本實錘）／壓到 0.1 過擬合（R1 dlf_fit 最差＋Ricky 經驗）→ 目標＝
  從未測過的中間帶（fit_loss ~1–3）。
- **機制（Ricky 設計）**：`mode: adaptive_window`——每輪 elite 訓滿視窗頂 hi、沿 log2 階梯 [hi/16…hi] 快照
  member0；held-out argmin **區位**決定滑動：連續 patience(3) 次落「**上二階**」（hi 或 hi/2，**不必貼頂**——
  最佳點上方保留至少兩階冗餘，Ricky 2026-07-03 修正）→ hi×2、落「最低一階」→ hi÷2、中段 → 不動。起點 64、
  **上限 1024**（Ricky 定——爬到頂≈學長「破千」量級）、下限 8；×2 滑動使階梯 key 跨視窗重疊 4/5 →
  bucket EMA 沿用；held-out 鐵律不變。
- **ensemble 5→3（R5，Ricky 定）**：省 SM 訓練成本（×0.6）；代價＝不確定性估計（trust/κ 的成員分歧）略粗。
  （R4 期的「保持 5」決策是當時脈絡；R5 起以 3 為準。）⚠ R5 vs R4 因此是**兩個實質變更**，歸因寫整包。
- **已知代價（誠實）**：live SM 過衝最佳點（等衡 hi≈4–8×argmin，偏成長）；成本單位＝1 epoch＝elite_n 步：
  R4 實測 elite ~90–120 → 起點 64 ≈ 2.2 萬步/輪（幾十秒）、**爬到頂 1024 ≈ 35 萬步/輪（可能與 HFSS 同量級）**
  ——正式機務必盯 `time` 欄，失控把天花板降 256/512（一行 config）。順手 `replay_size 256→512`。
- 實作：`WindowSMTrainController`（training.py，繼承 Adaptive 共用 probe_stats）；configs `single_r5_*`；
  取代 scratch 裡的 fixed-K 案與 probe-round 案（M 點平均若雜訊仍主導再上，見 round-05 §6）。

## 曲線 x 軸綁「真實 HFSS 模擬次數」（2026-07-02）
- **定案**（Ricky）：loss/worst_margin 曲線要跟**實際模擬次數**綁定，不用 epoch——epoch 含 cache 命中/skip，
  跟模擬預算對不上、判讀會困惑。
- **實作**：新欄 `hfss_calls`（累計真實模擬；cache/skip 不加；dense 每 epoch 落、斷點續跑續舊值，舊 run 用
  sm_gap fresh 標記數回填 seed）；`run_curve`（benchmark/round_report 共用）x 軸改用它、cache 命中收斂成一點、
  舊 run 回退 epoch；TB index 群組同步。`--at` 語意隨之變成「HFSS-call 預算」（跨 run 對標更公平）。
- **限制**：跑一半的 run 要等下次重啟（表頭遷移）才開始有此欄；之前的列由 fallback 撐。

## 自適應 SM 訓練量：機制 + 旋鈕定案（2026-07-02）
承下「SM 線上訓練強度」的方向，機制與旋鈕定了（**設計方向定案，實作時可微調**）；工作筆記見 [scratch.md](scratch.md)。

## 自主續輪授權（2026-07-12,Ricky:「我預期你會做這件事,除了 22 也許 23 24 25…」）
- **定案**：round 收檔後 Claude **主動**依證據開下一個 round（開檔＋判準寫死＋發車）,不等指令——
  批內循環與跨輪接棒都自動。方向選擇從「等核准」改為「**宣告制**」：新 round 的假設與判準在對話裡宣告,
  Ricky 可隨時否決/改向;沒否決就跑。
- **不變的護欄**：①每輪判準發車前寫死（docs/log 紀律）②紀錄級一律公證（鐵則）③check-dup 必跑
  ④兩類事仍停下等人：判準寫死的「回報討論」節點（如全臂連兩批死＋近王斷產）、判準沒預料的異常
  （.fail/數字對不上/批跑不完）。⑤期望管理用 /gain-check 三層帳,防過早悲觀/樂觀。
- **跨 session 接手**：worker 是 NAS 自治;「腦」的狀態在 ONGOING＋round 檔＋memory——
  新 session 接手先讀這三處,從 jobs-ls/NAS 真相繼續,不重問方向。
- **↳ 擴權（同日,Ricky:「你也可以自己建立新的臂」）**：新臂設計權下放——Claude 可自行發明實驗臂,
  但走**新臂協議**：①假設必須指向具體證據（資料/機理,不憑空）②先導配額制（首批 ≤15 筆,活了才加碼）
  ③存活判準隨臂發車前寫死（6% 基準或劑量反應）④宣告制照舊（對話裡講清楚 what/why/判準）。
- **↳ 漸進式成長條款（同日,Ricky:「允許漸進式的增長,前期很爛沒關係」）**：**分布外探索臂**
  （de novo 級,與現有資料分布斷開）不適用 6% 生死線——改**學費預算制**:固定投資額度（N 批）,
  前期 KPI=進步趨勢（best 指標逐批/區域 SM held-out ρ 逐批）,達標（三標）=畢業轉正,
  預算燒完=帶趨勢回報使用者裁決,不自動處決。首例=R23 D 臂（5 批×12,sm_harvest→sm_denovo）。

## 戰略定調：停滯=暫時,常升目標=探索範圍×資料量（2026-07-12,Ricky）
- **信念（Ricky）**：學長歷史紀錄中有帶外壓很低的**碎片化 pattern**（因太破碎+有弱點未採用）——
  「正確的 pattern 一定在那裏」。現階段停滯只是暫時;資料收集效率已大幅提升,
  未來階段能訓出更強的模型解決它。⚠ 佐證權衡:R20 F 碎片臂 0/85 蓋棺是「以當時模型+算子」的結論,
  不排除更強模型解鎖——這正是常設 D 臂+自產資料的存在理由。
- **定案**：不焦慮單批停滯;**每次總結必報 L0 常升目標**（全史真值筆數/方向圖/探索類佔比,
  gain 儀表 L0 節）——確保永遠有一個在提升的目標。
- **配套（同日）**：worker 自產 tier-2（`--selfgen`,預設開）——佇列全空時從歷史翻 bit+查重自產資料,
  任何正式 job 入佇列即讓位;**HFSS 從此制度上不可能停**。

## 降根計畫（2026-07-13,Ricky:「降低血統比例」＋「把提案都整理成規則」;R24 起生效）
- **診斷**：兩池稅治姓氏不治根——深血統 ~90% 樣本可回溯少數池根（credit:五紀錄鏈三條同根 g1_038）,
  同根局部變異收斂到相似極小值=「相似結果打轉」的結構性成因;可用帶外三批打轉為實測面。
- **三招（判準已寫死於 round-24 §1,proposed）**：
  ①**根多樣性稅** `--root-cap 0.4`（select 內沿 source_id 走到池根,單根 ≤40% 已接受樣本;
    三標率跌 >50% 即回調 0.5）——治本招,打「同山坡面」;
  ②**池外梯度**：D 12→20/I 資訊臂 12 上線/W 4→8/novelty 開（席位探索比 63%→~72%）;
  ③**sm_denovo 節點**：D 資料 ≥36 訓第一版,與 harvest 底座在 D held-out 對決——
    池外從「繳學費」升「有導引開採」的引擎,深血統比例下降靠它。
- **保底（反面約束）**：王朝鄰域 ≥25 席（近王產線=房租）;M 對照 ≥25（前瞻統計）;
  池的引力有戰績（R6:池贏 uniform ~5dB;b1 雙王）——降根是梯度不是懸崖。
- 預期軌跡:深血統 90%→78-82%（R24 內）→ sm_denovo 可導引後續降。

## 探索誘因包（2026-07-12,Ricky:「下一輪開始有相關的誘因」;R24 起生效）
- **A 資訊臂 I（12/批）**：兩 SM（本版 vs harvest 底座）預測分歧 top＝主動學習
  （query-by-committee）;KPI=模型更新量,非三標率。`select-r23 --i 12`。
- **B 新穎性紅利**：選批鍵折扣 λ·min(d_hist,20),λ=0.02（上限 0.4 dB）——每條臂都帶探索傾向,
  蓋不過真實差距。`--novelty` 開。
- **C 配額股息**：每批收檔按計分微調下批名額 ±6 席;**計分=當批效率 40%＋血統貢獻 40%＋新穎產出 20%**;
  保護=M 對照臂 ≥25（前瞻統計）、學費臂（D）鎖定不入市場。R23 期間純報表校準,R24 接管席位。
- **D 血統貢獻分**：紀錄沿 source_id 鏈回溯,祖先出身臂記分（`analyze credit`）——探索延遲報酬記帳。
  校準首跑即翻案:oobharv 11 分居冠（當批效率最爛的臂,蓋了兩條王鏈的祖先梯）＝
  「按當批效率殺臂會殺錯人」的實證,股息制的存在理由。

## 價值軸修正：過線後帶內餘裕邊際價值≈0,收益軸=帶外（2026-07-12,Ricky 定調）
- **定案（Ricky）**：「對 S11 與 Gain,只要過標之後額外的收益都沒有這麼重要了,壓低帶外才會帶來收益。」
  ⇒ 目標函數從「wm 最大化」改為**字典序:①三標硬閘門 ②oob_bad 最小化**。
- **不變**：「永不拿帶內換帶外」（2026-07-07）照舊——帶外只在過線者之間比;三標仍是硬閘。
- **保留（Claude 提,製造穩健性）**：可用解保留 **wm ≥ +0.15 buffer**（R11 公差:缺陷存活=margin 函數;
  +0.20 存活 56-72%、+0.03 級近全滅）——刀鋒王（如 o6_001 wm+0.00）當科學紀錄,不當製造候選。
- **生效範圍**：紀錄優先序即刻（帶外王=旗艦頭銜;margin/帶內紀錄降為參考點）;選批鍵 b4 起
  （pred_wm ≥ buffer → pred_oob 升冪主鍵）;R22 b3 判準已寫死照舊跑。gain 儀表補 oob 階梯。

**機制**：用 held-out 的 fresh HFSS 點量 SM 泛化，自調訓練量，取代固定 epoch / 逼 min_loss。
- 一輪 SM 訓練沿途存數個權重快照（採樣不同訓練量）→ 用「這一輪產生 pattern、剛過 HFSS 的 held-out 點」評快照
  → 「泛化 vs 訓練量」曲線的**形狀**給「加 / 減 / (學不動)」。
- **held-out 鐵律**：只能評「產生該點的那段（先前）訓練」的快照；**絕不可**評「正在對該點訓練」的快照
  （否則洩題 → 假結論『永遠多訓』）。
- **K=1 + EMA**：每輪一個 held-out 點就夠（配對比較抵消點難度），噪聲用 EMA 吸收、致動器慢走。

**旋鈕定案**：
- **ensemble 保持 5**（探測已與 ensemble 脫鉤，不必為省空間縮）；**訓練量探測只用 member0**（各成員同架構
  同資料、只差 init 擾動 → member0 的曲線代表全體）→ 快照只存 member0、~5 份/輪，與 ensemble 大小無關；ensemble(5) 負責 uncertainty。
- **判準＝配對 + 每桶 EMA + argmin**：同一 held-out 點上比各快照相對誤差（配對、抵消點難度）；各快照誤差
  按訓練量分桶、跨輪 EMA → 取平滑曲線 argmin 當目標訓練量（EMA 目標、慢走）。**護欄**：argmin 落快照範圍最外側
  → 用斜率方向把探測範圍外移/擴，別被固定範圍卡住。
- **快照 = ~5 份/輪、偏早密（log-ish 間距，knee 通常在早期）**。**存哪（實作定案，比原磁碟版更簡）：留記憶體**
  （`prev_snapshots` dict，每輪覆蓋上一輪）→ 零磁碟、零清理、resume 最多少一次 observation。member0 state_dict
  ~16MB×5≈80MB steady，可接受。（2026-07-02 實作時微調，取代原「存 `checkpoint/probe_snapshots/` + 滾動清理」。）
- **實作落點**：`sm_train.mode: adaptive`（新 mode）；`adaptive` 區段旋鈕 {snapshots, epoch_min, epoch_max, ema}；
  `AdaptiveSMTrainController` 在 `training.py`；快照/評估在 `surrogates.py`（`train_by_datas(snapshot_epochs=)` +
  `eval_snapshot`）；只探 member0。新追蹤訊號：`sm_train_epochs`/`probe_argmin`/`probe_min_err`/`probe_max_err`/`elite_n`。

**發車前健檢補記（2026-07-02，R4 派工前）**：
- **⚠ 歸因誠實**：`adaptive` 相對 `dlf` 其實換了**兩個機制**——除了 elite 訓練量 1→自適應，**也拿掉了對最新點
  的 `train_one_data` 激進單筆擬合**（單筆擬合＝已確認反模式；最新點反正在 elite 裡照訓）。R4 vs R3 的差異
  歸因要講「SM 更新規則整包換」，不能只說「訓練量自適應」。
- **死鎖修復（模擬實證）**：原控制器早期雜訊把 target 壓到 ≤5 後永遠救不回（整輪默默退化回 dlf）。成因×2：
  整數 target 上做 EMA、1.3× 加碼被 round() 吃掉（低訓練量吸收態）；argmin 在歷史所有桶選、範圍外的過期低值
  永遠投票。修法：**target 內部存 float**（對外才取整）＋ **argmin 只在本輪有觀測到的桶裡選**（bucket EMA 只
  平滑值）。修後三情境驗證：死鎖恢復、sweet spot 收斂、純雜訊保守退低（＝dlf 現狀＝安全失效；監測法：
  `probe_min_err≈probe_max_err`＝曲線平＝探測沒資訊）。
- **斷點續跑**：控制器狀態只在記憶體 → 重啟歸零重學；`seed_target()` 用 metrics.csv 最後一筆 `sm_train_epochs`
  續 target（bucket 重學即可）。HFSS 當機重啟頻繁，沒這個 500-epoch run 的自適應會一直被打斷。

## SM 線上訓練強度（2026-07-02）
- **每輪只訓 1 epoch（現行 `mode:dlf`）偏少** ✔ — SM 欠訓、系統性低估 HFSS ~3–4.5，是 R3 三臂 plateau
  的共同上游根因（gap 高 → `trust_t` 被鎖在 0.05、永不進入利用）。佐證＝R3 健檢。
- **「硬逼到 `min_loss=0.1`」實測效果也不好** ✔（Ricky 經驗）— 過度擬合 buffer＝記憶非泛化、又慢。
  → **方向不是「訓更多／逼到 0.1」，而是「訓到泛化最好」**；具體走「跨輪自適應訓練強度」（發展中，見 scratch）。
- **rollback / keep-best 用處不大** ✔ — 先前已討論 + 程式 2026-06-28 已移除 rollback（原實作還有 off-by-one
  等 bug、實際近 no-op）。→ **不重拾舊 rollback**；好解回收若真要做，另想機制、不走那條老路。

## 目標層級定案（2026-07-07，Ricky）
- **硬約束（一定要滿足）**：帶內 worst-margin ≥ 0（S11＋Gain）**且** rad ±45° ≥ 0（phi0/phi90 雙切面）＋可製造
  （全件 ≥4px、零粉塵）——四關任一不過即出局，無交換空間。
- **加分條件（tiebreak,永不換帶內）**：帶外選擇性——帶外與帶內**反向**：S11 貼 0（全反射）、Gain 越負（不輻射）。
  指標＝`oob_metrics`（遠帶外兩側各 4 點,排除過渡點;惡度=Gmax−S11min,越低越好）。
- **實作**：批次選擇一律字典序——①硬約束（R11 起含公差穩健:erode/dilate 三態皆三標）→ ②帶內 min-margin
  最大化 → ③帶外惡度最小化。SM 預測全曲線,C 臂導引免費帶入 ③。
- 現況基線：八冠軍 oob_bad 9.0-11.5、低頻裙擺（24-25.5）為主破口;選擇性榜與 wm 榜近乎反序。


## 戰略轉向：從「刷三標數量」→「收斂＋破單一化」（2026-07-08,Ricky 討論後）
- **觸發**：R11 後去重 50 個互異三標過,但全 1 家族（w17 高原,Hamming>60 聚類）、中位 wm +0.07、僅 8 公證。
- **判定**：原目標「找任何可製造三標解」已徹底解決（過度解決）。再找第 51 個 w17 變體邊際價值≈0。
- **兩條真瓶頸**（R12 起）：
  1. **收斂**：選要送製造的穩健冠軍——標準是**局部缺陷存活率**（tol:c21 10/18 遠勝薄冠軍 1-2/18）,不是 margin
     （+0.20→+0.22 已逼近雜訊）。公證後加冕。
  2. **破單一化**：50 全同族＝單一培養皿風險。第二座山頭（非 w17 家族）比第 51 個變體值錢。
- **含義**：margin 競賽停手;HFSS 預算轉去公證收斂 + 找第二家族。組數階梯(c25)雖贏但仍 w17+裝飾、未離高原。


## 設計語言：組件參數化（2026-07-08,R14 收檔定案）
- **像素級正式退役**（Ricky 定調,R14 實證兌現）:優化算子不再翻隨機像素。
- **pattern 的設計語言**=下主件（匹配底座,尺寸已最優、鎖定）＋雙翼（帶內引擎:+6dB 遞減,尺寸刀鋒級敏感,
  同時付 rad/帶外代價=三標張力的載體）＋可選小塊（rad/選擇性微調旋鈕,甜蜜點 4-5 塊,R13）。
- **算子分工**:機理探針=occlude/ablate/resize（塊級/圈級）;優化=add_block/小塊移除（2×2~3×3）;
  公差=erode/dilate/edge-flip。generator 先驗照此參數化。
- 證據:R14 ablate（翼合值 6dB/首翼 4.4/次翼 1.5;去翼 rad+1.3、oob−2.8）+ resize（main+1圈 −1、wings+1圈 −19;
  oob 隨 main 增大爆炸）+ R13 組數 + R11 搭橋/occl2——四探針交叉一致。


## 敘事轉向：線上學習＝工具，agent + human-in-the-loop 共同優化（2026-07-08,Ricky 定調）
- **定調（Ricky）**：專案敘事從「單一優化的線上學習」轉為「**線上學習是工具箱一員**,由 agent 與
  human-in-the-loop 一起進行優化」。這是 2026-07-03「研究定位」條目（線上學習=方法論中的局部開採階段,
  不是研究本身）的自然延伸——當時把線上學習降為階段,現在把「誰在調度這些階段」補上：agent+人。
- **文獻背書（docs/reference 三篇,Sengupta 組,詳見 docs/reference/README.md）**：
  - 2026 SSC Magazine＝錨點：終局架構明畫「Foundational Model + **Human Inputs**」（Fig 1b）、
    L1–L5 自主性光譜（線上學習=其中一級的一個工具）、"AI is a collaborator, not merely a
    brute-force optimizer"、AI 現階段強項=「加速探索/揭露 tradeoff/卸載重複優化」（p.66）。
  - ISSCC 2026 13.2＝方法佐證：「spec→design 一對多,最終取捨由人的主觀因素決定→架構演化可控性
    是 AI 設計被採用的前提」;designer inputs + hybrid design/optimization 明列在流程圖。
  - 2024 MWSCAS＝對照組：全自動 push-button 路線,明寫排除人工介入——立靶用。
- **誠實邊界（novelty 空間）**：文獻的 human-in-the-loop＝事前注入約束/prior＋事後在等價解中挑選;
  "agent"=RL agent 非 LLM agent。「agent 與 human **迭代迴圈**共同調度工具（池篩/結構規則/HFSS 驗證/
  線上學習收尾）」文獻沒有現成先例——這正是我們可以claim 的一步,但表述時別把兩種 agent 混同。
- **對外表述紀律（沿用 07-03）**：說「線上學習的正確角色是局部開採、是工具箱一員」,不說「線上學習沒用」。
- **回看**：我們實際工作型態早已如此（Ricky 出假設/定調 → Claude 分析/生批次 → HFSS 公證 → 回灌決策）,
  這次是把它從「工作方式」升級為「研究敘事本身」。


## 批次設計原則：每批 ≥30 筆,冗餘額度養探索（2026-07-09,Ricky）
- **規則**：估實驗數量一律 ≥30 筆/批。核心假設的樣本排完後,剩餘額度**刻意**拿來測:
  ①其他還沒排上的想法 ②「不一定會好」的探索（低期望高方差的賭注）。
- **Why**：HFSS 批次的固定成本（開機/派工/收檔判讀）攤提後,邊際筆成本低;小批（<20）浪費機時窗口;
  且「覺得不會好」的樣本常帶來意外資訊（R15 隨機臂 25% 過線、g14 +0.40 都是例子）。
- **How**：select-* 生成後若 <30,問「還有什麼半熟想法可以搭車」再發車;scratch 候選區就是搭車名單。

## 帶外戰役定案（2026-07-09,R17+R18 收檔）
- **三標內帶外地板 ≈9.0**：紀錄 c18_sm 9.04（跨店雙響應）> x20 9.15 > vb43 9.24（皆公證三標）。
- **低側（24-25.5）雙判死**：①w17 家族內「物理可動但不可負擔」——hslot 有乾淨劑量反應
  （lo 3.74→2.59）但每壓 0.3 付 ~1 wm;②池頂低側族「粉塵諧振本體」——救援 0/20,
  對稱化保低側殺帶內（wm −16~−24）/除塵殺低側（lo 回升 +1~+2.4）完整互斥（R7 定律帶外版）。
  - **★ 適用範圍註（2026-07-14,Ricky「他的帶外很好」對帳促成）**：雙判死=「**王朝家族內**不可壓」
    ＋「池頂族**用當時的算子**（對稱化/除塵）救不活」,不是低側物理不可壓——學長碎片族低側對比
    6.1-8.7 dB（t09 lo −4.47）vs 我方全部冠軍 **0.4 dB（形同全通,高側特化）**;兩家族各壓一側。
    新算子（R27 網架臂:半凝聚保網布+塊級修 rad）重開此案;獎品=可用帶外 9.0→7.x 級。
- **帶外構造法收斂**：可負擔旋鈕只剩高側（中央塊/vslot）;帶外角色回歸 tiebleak,
  判準拆側（低側=體質常數）不改指標、只改解讀——lo 欄持續 track 當機理讀數。
- **附帶新機理**：hslot=rad 劑量旋鈕（+1.0~+1.7,史上最大 rad 增益,代價 wm −1.5~−3）=rad 規格收緊時的保單;
  中央帶金屬**集中 ≫ 分散**（等金屬 12px:1 塊 +0.24/3 小塊 −2.6）;中央塊尺寸峰=3×3。
- **鐵則戰績 +1**：b20_k4 舊單次 +0.32 → 公證 3/3 −0.19（第二次假象攔截;2026-07-06 前 ref2 時代
  單次值一律視為未經證實）。
- **模型線觸發成立**（預註冊條件命中）→ R19=SM v5 重錨+GA over 組件-算子空間（Ricky 2026-07-09 方向）。

## 帶外地板下修補註（2026-07-11,R20 gen1 公證）
- 「三標內帶外地板 ≈9.0」下修為 **≈8.8**：vg0338_c18（c18 翻 1px,三次一致 8.84/8.84/8.86）。
- **張力結論不變**：低側 3.68 紋絲不動（鎖死機理如故）,0.2 dB 改善全在高側（hi −4.14）——
  地板的物理本體（低側裙擺）沒被打破,是高側旋鈕在 c18 鄰域的細粒度兌現。
- 方法論註腳：R17/R18 構造法沒找到的 0.2 dB,被 R19 隨機變異＋公證制度撿到——
  「隨機性是引擎,制度是濾網」的又一例。

## 戰略換軸：資料多樣性 → SM 準度 → 變現（2026-07-15,Ricky 拍板「G 臂要繼續做…整體的想法應該是資料的多樣性,然後慢慢強化 SM 的準度」）
- **定調**：王系血統開採降權（邊際見底實證:margin 王兩週 +0.01/可用帶外零推進連 4 批/王系鄰域樣本
  ikpi ±0.05 vs G 臂 adversarial 樣本 **+4.50**）;主軸=多樣性資料教 SM → SM 可信半徑擴張 → champ 帶
  （近錨梯度）變現。**終極目標不變（找好 pattern）;普通 pattern=制度性探索成本,不需每批辯護**。
- **Round KPI 五軸面板**（每輪收檔必報,跨輪畫曲線;取代「換王」單軸敘事）：
  ①**SM 準度（主 KPI）**=held-out 誤差＋前瞻 ρ＋G 臂各帶 adv 率——輪的成功=此軸有斜率;
  ②覆蓋/多樣性=唯一樣本增量＋新血比例＋近王比例/d_dyn（反自餵儀表）;
  ③**整體水位**=批 wm 中位/P90＋**帕累托前緣增量**（wm×rad×oob 非支配解 +N/批）＋作戰區密度
  （wm≥−1 累積量）——回應「不能只看王,要看整體提升」;
  ④變現=三標數/紀錄推進（champ 帶+守成臂 ~30%;有就公證,零推進不焦慮）;
  ⑤健康=error 率/機器事件/ikpi。
- **機制調整**：錨點池王朝/冷支抽樣 70/30 → **40/60**（--dyn-frac,R29b3 起）;四層多樣性機制不變
  （root-cap 0.6/simcap 0.12/新血注入 27%/全歷史查重）。
- **平台期預期**：紀錄榜可能數輪不動（R25 先例:三標率↓換 L2b 效率翻倍）——KPI 換軸後這不是失敗訊號。
- **Why**：SM 可信半徑=系統能力上限（b1 實測 champ d≤25 半可信/d≥60 全崩）;文獻定調 active learning
  =治本;「SM 引導在某預算後勝過 random」=論文最強潛在翻案,adv 率曲線=直接量測。
- **How**：工具=sm_reanchor 自動報 held-out（append docs/kpi.csv）+analyze batch 覆蓋段/水位段;
  每輪 §4 貼五軸面板;輪假設判準照舊（§1 寫死、≤3 批）。

## 反馬太四機制（2026-07-15,Ricky「以第一性原理想,流程裡加什麼取代人為手動 check」→「全都執行」）
- **第一性診斷**：馬太效應=四環正回饋（選樣偏自信區→訓練分布更偏→held-out 從偏分布切=溫度計歪
  →SM 的無知不會自己進迴圈）;人眼 check=人肉負回饋,制度化=每環插一個負回饋。
- **機制**：①**恆溫器**（analyze batch 行動⑥:批內 NN<40 或對歷史 NN<15→自動印下批配額/稅調整建議）
  ②**密度反權重**（sm_reanchor:訓練樣本權重 ∝ 1/局部密度〔Hamming<30 鄰居數〕,複製式實作;
  均勻 ×8 過採樣退役——v35 起）③**分層 held-out**（kpi.csv 加 err_near/mid/far 按 d_dyn 三帶——
  「遠域誤差沒降=馬太在贏」一眼可讀）④**誤差錨點池**（analyze batch 寫 error_anchors.json
  =|pred−real| top 8,select 自動吸收進冷支池——錯哪補哪）。
- **Why**：固定探索稅=開環閥門,馬太變強不會自動加重;誠實溫度計（③）是其他恆溫器的校準前提。
- **How**：全自動,人只看三個燈（恆溫⑥行/kpi.csv 遠域列/誤差錨清單）;⚠②改變 v35 起訓練分布,
  分層 held-out 首讀驗證「遠域降、近域不升壞」,壞了回滾均勻過採樣。

## SM 準度目標線＋量測制度（2026-07-16,Ricky 拍板「1、2 照你的;3 用 b,資料量起來了」）
- **可導航門檻（KPI① 的終點線）**：champ 帶 adv 率 **<50%** ∧ 近域 held-out 誤差 **<0.8 dB**
  → 開始信 SM 排序;遠域 **<1.5 dB** → 可放膽外推。現況（v38）:adv 74%/近域 1.28/遠域 2.56。
- **凍結基準集**：held-out 隨批擴張=尺在漂——`configs/heldout_frozen.json`（v39 首跑自動凍結）
  =跨版固定尺;kpi.csv 加 frozen_med/frozen_far 欄。
- **rad 頭制度合訓（b 案）**：rad_head2 化石問題（訓後九版沒吃新方向圖=前瞻蹺蹺板頭號嫌疑）
  → 每版重錨自動配訓同期 `rad_headNN.pth`（全量方向圖 9k+,30ep;--no-rad 關）;
  kpi.csv 加 rad_rho/rad_mae=rad 準度曲線。select 發車 --rad-head 用同期版。
- **Why**: 有目標值曲線才有終點線;方向圖資料量已支撐全量重訓（Ricky:「資料量也起來了」）。

## SM 利用率雙升級（2026-07-16,Ricky「1、2 都上」——「我們有利用好 SM 嗎」討論的落地）
- **①漏斗放大**：gen --oversample 3（每帶生 3× 候選,SM 按帶目標挑 top:三標帶=maximin(wm,rad)/
  oobp=帶外壓低）;select 常規臂候選池 ×2（core 12→24 倍/cold 8→16 倍）——SM 打分免費,
  9k 資料的 SM 該篩更大的池（缺口:G 臂原「生成即入選」零篩選）。
- **②ensemble 不確定性（記錄版）**：每版重錨加訓 2 顆異 seed 半程成員（epochs/2）→ select 三成員
  pred_wm 的 std 進 manifest（pred_std）;**第一版不進選批鍵**——判讀驗證「std 分桶 |pred−real|
  校準」成立後,再上「高信心變現/低信心探索」分流（文獻 active learning 的原則性實現）。
- **Why**: 批次線對 SM 的利用「深度升維（inversion/adversarial）但廣度掉隊」——更新頻率降 40×
  可接受,漏斗與不確定性是真缺口。

## 王朝重定義:表型取代血統（2026-07-17,Ricky 提議+驗證定案）
- **定義**：王朝=**表型**,雙判——①結構判（零成本,生成端可算）:blockmap 分割（高斯 σ0.8×閾 0.6）
  後「底部 1 個大組件（≥60px∧質心 row≥12）＋上半 ≥2 個中組件（≥12px∧質心 row<10）」;
  ②功能判（收檔審計）:左側帶外 gain 壓不下去（oob_gain_max_lo>0）。
- **驗證（2026-07-17,各抽 300）**：王朝家族結構判 **100%** 命中/功能判 96%——結構⇒lo 因果實錘;
  中繼帶 42%/41%（手術系結構半保留但 lo 已修——**過濾須豁免中繼家族**）。
- **★ 戰略級發現**：非王朝血統的「其他」族群 **81% 也是王朝結構、85% lo>0**——全史約八成
  困在王朝結構;血統凍結（DYN token）擋不住**結構收斂**（M/O/I 臂親代非王朝血統、結構同型）。
  低側壓不下去+PCA 王朝大陸過大的真因。舊血統口徑另有實測缺陷:族譜追根要遞迴
  （margin 王 o29b2_011 追 9 層根=g1_038 王朝;淺層 token 判定會漏）。
- **How（R33 操作化）**：select 生成端結構判過濾/降權（中繼家族+lo 實測<0 者豁免）;恆溫器加
  結構多樣性軸;PCA 網格佔據率進判讀;novelty 鍵擴 PCA 距離。Ricky 期望=**PCA 分布均勻化,
  多種 diversity 檢查都要均勻**。

## 影子 CNN 對決制度＋首兩讀型態（2026-07-17,R32 搭載）
- **制度**：每版重錨平行訓 CNN 挑戰者（同鍋 ds,從零 2× epochs 補償 MLP 的 harvest 預訓）;
  三尺盲測=尺1 凍結基準（訓練時,kpi_shadow.csv）/尺2 盲測誤差+前瞻 ρ/尺3 adv 率（判讀時
  analyze batch 雙模現場打分,**必須重錨前跑**）;**連兩批三尺全贏→轉正**（zoo "cnn" 成預設+
  全鏈換錨）;輸→降級異架構 ensemble 成員。
- **首兩讀型態（R32b1/b2）**：「**CNN=排序器,MLP=回歸器**」——尺2 ρ CNN 連兩批 3 倍碾壓
  （+0.592/+0.556 vs +0.144/+0.194）;尺1 絕對值 MLP 連守（凍結 1.15-1.39 vs 2.7-3.0）但
  遠帶 CNN 連三版反超;尺3 CNN 兩批零吹牛（vs MLP 67-83%）。未達轉正。
- **候選（R33）**：混合鍵——select 排序摻 CNN、絕對值門檻/LCB 用 MLP。卷積空間先驗在
  分布外泛化的證據已三讀,架構升級方向確定,只是路徑走「混合」比「替換」穩。

## 合作方法三修正（2026-07-17 深夜,Claude 提批評→Ricky 修正框架+「全部都做」）
- **①SM 角色重定調（Ricky）**：SM 的工作=「找有潛力的域」（排序/導航）,不是「預測改善量」
  （回歸）——「用 1.3dB 尺量 0.4dB 目標」的批評框架錯,排序力才是導航 KPI。落地:五軸面板
  KPI① 主讀數=**前瞻 ρ/期望閘（排序力）**,絕對誤差降輔助;影子對決正是此框架的驗證
  （CNN ρ 三連勝=更好的導航器候選）。
- **②顯微鏡包（窮舉×防陷困折衷）**：每輪**限一夾 25 筆**——單一高價值錨 d=1 鄰域全枚舉
  （625 變體全打分=小空間完備）→ CNN 排序 top 25 送測（SM 用強項）;**錨每輪輪換**（防陷）。
  窮舉管不漏、CNN 管排序、輪換管不陷。首包=R33b2 搭載（錨 g32b3_034）。
- **③burn rate 冷數字＋單機制紀律**：records.json 加 history（推進事件 append-only）;
  analyze gain L4 行=近 14 天推進次數+乾涸天數,**防「工具勝利掩蓋結果停滯」**（對稱於
  gain-check 防過早悲觀）。R34 起**新機制一次一個**;M 臂升格「凍結對照臂」（機制變更永不碰,
  跨輪天然 baseline）。8 月中設「寫作優先/實驗降頻」切換點討論（12 月 deadline 倒推）。

## Tier 架構:多線並行的線上學習（2026-07-21,Ricky 提議+定案）
- **三層**：**tier 0=插隊層（prio 1）**——爬山鏈 daemon（`dedust chain`:錨 d=1 純隨機包→判讀→
  換錨迴圈=線上學習的閉環結構、訓練搬到批間重錨〔開發機做,HFSS 零卡頓〕;判準寫死於 CLI --goal,
  帳=docs/chains/*.jsonl）;tier 1=批次（prio 3,教材+多樣性+對照）;tier 2=selfgen（覆蓋填縫）。
- **為什麼不是真線上學習當 tier 0**：train.py 版每 ep 278s 大頭是訓練=插隊層會佔 HFSS 機時;
  爬山版=同樣閉環、零佔機。真線上學習若復跑=一台機的長駐獨立實驗,不進插隊層。
- **治理（防分支發散,Ricky 關切）**：鏈數 ≤2 條並行（一機一鏈,留一台全職 tier1/2）;
  `analyze tiers`=使用率/ROI 冷數字面板（首讀:tier0 三標率 21.3%>tier1 10.5%>tier2 2.0%
  =各層職責分明）;鏈判準寫死+dry 2 包自動收鏈。
- **臂改版同步定案（R34b1 判讀）**：champ 梯度帶退役（王鄰域外四連敗:bridge/中繼/B泵/champ-I
  ——近錨開採一律枚舉/翻bit,梯度只留 free/oobp 遠走帶）;I 臂 16 席=甜蜜點（24 席稀釋實證）;
  D 16→8/W 12→8;發車前驗錨結構=新 SOP（R34b1 L 錨組 92% 王朝=選錨失誤教訓）。

## 多軌 SM 路線圖（2026-07-22,Ricky「多軌 SM=性能提升關鍵點」+「循序漸進」拍板）
- **三維度**：A 域專家（王朝/中繼/selfgen/海峽——微調子集;證據=可信半徑+B 泵高估）;
  B 軸判別器（pattern→wm/lo 標量直接回歸,比 51 維曲線好學）;C 角色分工（生成導引/選拔排序/
  **校正器**=per-域誤差線性修正）。
- **吞吐三解**：微調代替全訓（--expert 已驗證,80 分→3-5 分/顆）;懶訓練（發車時才 refresh）;
  訓練佇列化（開發機空檔自動補訓）。
- **治理前置**：SM 註冊表（configs/sm_registry.json:每顆=職責/量測尺/進鍵狀態/退役條件）
  +analyze sms 面板——NAS 已百顆 .pth,無註冊表的多軌=垃圾場。
- **落地順序（循序漸進）**：①R35 註冊表+校正器（最低成本,救 B 泵型偏差）→②R35-36 域專家
  三顆試點（中繼/selfgen/王朝對照;判準=域內 ho ρ 贏全域才上崗）→③R36+ 軸判別器 vs 影子 CNN
  排序對決。組合拳=B×排序/A×爬山鏈/C×變現臂,各管一用途不互相取代。

## Tier 再平衡規則（2026-07-22,Ricky「tier 0 越來越有戲就持續降低 tier 1」）
- **方向**：機時配置隨 ROI 動態傾斜——tier 0（鏈制）產出率持續壓過 tier 1（批次）,就逐步縮批次、擴鏈。
- **量測**：每輪收輪讀 `analyze tiers` 產出率比（同口徑=三標率或合格率;R34 首讀 tier0 21.3% vs tier1 10.5%）。
- **階梯（收輪判,一次動一格）**：比值 ≥2 連兩輪 → 下輪批 75→50;再持續 → 50→25 地板+鏈上限 2→3
  （改治理條目）。**tier 1 不歸零**:批次臂是多樣性教材/王朝壓制監測/M 凍結對照的唯一來源——
  地板值留 25/批;若多樣性 KPI（批內/歷史最近鄰中位）因縮批塌縮,回退一格。
- **反向條款**：鏈連兩輪乾涸（軸枯竭/收鏈快）→ 機時回流批次,不留戀。

## 左右側拆帳紀錄制（2026-07-23,Ricky「單獨看左右側,持續往下壓,不要只活在這個 session」）
- **背景**：可用帶外紀錄 9.0→8.97（c3g1p01_24）被 Ricky 判「同型邊際推進無可比性」——左側 gain
  紋絲不動（新舊紀錄與錨全在 +3.7）。全史掃描實錘:882 合格解 lo 全擠 +3.68~+3.7（零壓制）、
  三標∧lo≤0 全史 **0 筆**;lo 可壓族（≤−2 有 2,133 筆,wm 最高 +0.42）全卡 rad。
  **根因=oob_bad 總帳蓋牌左側**——嫁接錨選擇/鏈 dual score/紀錄鍵全用它,左側不在任何目標函數。
- **制度**：①records.json 新增 **usable_lo / usable_hi**（合格門檻內 min 單側 gain;基線 +3.68 /
  −5.92=全史掃描單次,推進照公證鐵則）;②analyze batch 左右側拆帳段＋側紀錄候選行動行;
  ③chain 新 goal **lo/hi**（合格門檻內壓單側,掉出門檻 −99）;④鏈帳全 goal 記 lo/hi 欄。
- **價值序**：左側=主戰場（歷史零壓制的牆）;usable_oob 總帳型 0.0x 推進降級為次要事件
  （記帳不推播）。右側基線已 −5.92=歷史證明可壓,守成軸。
- **與 SM 配比的關係**（Ricky 問）：lo≤−2 樣本鍋內已 2,600+ 筆;先修尺（本節）,SM 側後續=
  lo 軸判別器（多軌 B 類,pattern→lo 標量）+域專家——R37 起排。

## ★修正:trade-off=王系經驗律,非物理定律（2026-07-23,Ricky）
- **原帳**：「rad↔lo 梯度不可兼得三確認」（R33）與「三軸同好全史 0 筆」（左右拆帳制掃描）曾被
  當成全域結論。**修正**：三確認全部量測自王系/王朝結構鄰域;全史掃描的母體本身王系主導
  （response PCA 實錘:整個資料集擠向同一系統聚落）——**結論範圍=王系內經驗律**。
- **反例證據（自家資料）**：學長池頂族 t09 系=lo −4.5∧oob 7.2∧wm 可用,只卡 rad（analysis-03）
  ——該系統剖面=lo 與合格不互斥,缺軸是 rad。學長系列亦有非對稱圖 rad 未達標但其餘達/極近（Ricky）。
- **換系統戰略**：增加其他系統比例/探索不同區域——手段:①selfgen 種子池結構過濾（tier2 轉產
  非王系資料）②response 反權重（訓練端自動配比）③harvest 重放分層保底④L 臂錨組換 t09/中繼帶族。
- **R37 雙主軸草案**：非王系系統 rad 最後一哩（接 c2rad −0.42 未竟）＋資料配比換系統。
  左側戰役的正解不是王系內壓左側,是**去左側好的系統把缺軸修完**。

## 結構準則更新:粉塵忽略×對角黏著規避（2026-07-23,Ricky）
- **粉塵軸=研究期忽略**（不作否決條件）——t07 池頂族因此重回合法錨/種子池（該族剩餘硬傷只有 rad）;
  可製造性收官階段再回收此軸。
- **對角黏著（對角線金屬塊）=主要規避準則**：analysis-05 證據=有對角橋三標 14% vs 無 36%。
  落地:①`diag_bridge(p)`（4連通−8連通組數,analysis-05 口徑）R36b3 起進 select 記錄鍵（diagb 欄）
  ②R37 判準寫死進罰分/過濾 ③種子/錨選擇偏好 diagb=0。

## 機制健檢反思（2026-07-24 凌晨,Ricky「再想一次,過合理的就動」）
- **物理紮實層（不動）**：wm/rad/buffer、左右拆帳（低側裙擺 vs 高側滾降=不同機理,拆帳比總帳更物理）、
  diagb（對角點接觸=電流奇異+製造不可靠,文獻通則）、鏡射增強（feed 中軸精確對稱）、d=1 微擾
  +graft 跳盆地、承重圖/翼=帶內引擎/網布載低側（最像物理發現的資產）。
- **經驗有效但降級認知**：dyn_struct=族群統計相關非機理;asym=臂間混淆已正確退記錄鍵。
- **★撤回的假簡化（反思紀錄,防重提）**：①「反王朝三合一」——三機制=同一政策在生成/選拔/訓練
  三關口,是一致性非冗餘;_dyn_pack token 清單=凍結尺分層參照系非過濾器,動=毀 kpi 時間序。
  ②「O 臂鍵換 lo」——合格池 lo 零變異（PCA 實錘）,現在換鍵=排序雜訊;**觸發條件寫死:批內合格
  候選 lo std>0.5 再換**。③「G free 縮席」——R37 已是 6 席（提案基於過期記憶）。
- **通過並執行**：④標量 rad 判別器（rad=全系統綁定瓶頸∧現任 rad 頭 ρ 0.09↔0.46 最弱儀器∧
  rad_margin 本來就是標量規格）——併 train-two 框架,pred_rad2 記錄鍵,M 臂前瞻對決舊頭,輸者退役。
  dual goal 標記 deprecated（新鏈一律 tri/lo/hi）。lo≤−2 門檻=慣例待 Ricky 定線（開放）。

## 里程碑層與聯合回顧制（2026-07-24,Ricky「增加紀錄點/偶爾一起回顧」）
- 新層=docs/log/MILESTONES.md（章節級:一章=一個戰略段落,已回填八章）——在 round（假設級）與
  decisions（原則級）之上補「時代感」;指向不複製。
- **回顧節奏**：每 ~10 輪或每逢戰略轉向收一章;Claude 起草＋Ricky 共同過帳＝聯合回顧
  （回顧本身也是反思機會——健檢/假簡化撤回這類自省記入 decisions）。
- 依據=git 密度:有效研究史 ≈7 週（2026-07 單月 388 commits=62%）,章節層防止日常顆粒度淹沒弧線。

## 考試制（模型上崗鐵則;2026-07-24 Ricky 背書「原本我也是這樣想」）
- **離線贏≠上崗**：任何模型/鍵要接管實權,一律「離線考試（held-out 三尺）→ 實戰面試（批線
  前瞻盲測,連兩批）→ 轉正」。證據=CNN 單 rank 反例（離線 ρ 0.75-0.81 全勝,實戰選拔 1/6 回退）
  +影子 CNN 正例（十讀+連兩批才進鍵）。
- 適用:回歸主鍵/排序鍵/判別器進鍵/罰分鍵——select 實權的一切變更。

## 資料擴展主軸=response 空間（2026-07-25,Ricky）

- **原則**：資料擴展沿 response 空間想（空洞反演/分布配比/response-novelty），
  **pattern 空間探索點到為止**——「pattern 的空間感覺不是很連續」（Ricky 2026-07-25）。
  物理：二值 625 維 pattern 的插值/混血不保共振結構，兩個好解的中點多半兩頭空；
  response 空間（S11||Gain）才是連續、可設目標、可導航的軸。
- **操作**：①pattern 島間橋接只做試金石（≤6 筆一次性）不成臂；②response 空洞反演
  =R40 新臂候選（目標=response PCA 稀疏區質心）；③雲內爬升走 selfgen 閒時便宜線。
- 與「戰略換軸（多樣性→SM 準度）」同軸：多樣性的正確口徑=response 覆蓋，非 pattern 花樣。

## 組級變異算子定案（2026-07-26,R41 收輪）

- **「組（8-連通金屬島）是比 pixel 更對的變異單元」——鏈線常駐定案**。判準兌現:
  c41grp2 p02 grp_grow 產出第三筆左側合格解+usable_lo 紀錄 −3.46（公證 3/3）,
  觸發「grp 半產合格解=直接常駐」;A/B 帳兩包 grp 平手/勝。
- 六算子=修邊(grow/shrink)/平移/刪/複製/新增/對角開關（骨架 >25px 凍結,diag 例外）;
  `chain --mutator group`（70/30 px 對照續帳）。**批線推廣待 A/B ≥6 包再判**（R42+）。
- 分組口徑：**操作口徑=8-連通**（9宮格含對角,Ricky 定義;變異單元）;
  4-連通=電學檢視口徑（對角拓撲開關假說;grp_diag 實測 p01 登頂一次,續讀）;
  closing/DBSCAN 口徑=整圖單一耦合體（不用於分組）。
- 左側家族結構學：骨架(193/80/30)+星座(≤10px×5-6);刀鋒解性質(微擾合格保持 0);
  skew 舵=9-10px 中件;對角清潔=fill 式可行 brk 式災難。

## 對角操作的組保持規範（2026-07-26,Ricky）

- **原則**：對角接點的任何操作（補實 fill／移除 brk）**都要符合組的規範——該組必須維持住**;
  口徑不只一種（4-連通/8-連通都算),因為還不確定哪種分組才是對的,所以**兩種口徑同時守恆**。
  實作＝`dedust.diag_clean()`:雙口徑組數皆不變才接受該步,否則跳過。
- **對角兩型（本規範推出的分類,R42 實證）**：
  · **橋接對角**（兩端 4-連通不通,8-連通全靠它）→ **一律不動**:補實=兩諧振器併一個、
    斷開=一組拆兩半,兩者都在改電路。R41/R42 清潔階梯全滅的真因（沒分型就一律動）。
  · **冗餘對角**（兩端另有 4-連通路徑）→ 可安全清（fill 或 brk,組數守恆）。
- **左側家族實測**：合格解的對角接點 17-19/20 是橋接型,可清的只有 3-4 個
  → **「組保持」約束下這一系的對角本質上減不掉**;要低對角得從生成端要求（新解不靠對角連接）,
  不是事後清潔。
- **指標盲點**：`diagb`＝k4−k8 **只數橋接型**（冗餘對角看不到,清完 diagb 不變）。
  可製造性追蹤要看**總接點數**;diagb 續當「結構性對角」鍵。
- ★**規範實測結果（r42s2,2026-07-26）：組保持也救不回來——7 筆合規清潔全掉線**
  （fill Δwm −0.16~−0.20;brk **Δwm −14~−16** 災難級）。即雙口徑組數完全守恆、只動 1-4 個
  冗餘接點,合格解照樣崩。→ **三次獨立實驗一致（r41s1 階梯／c41grp3 收復鏈／r42s2 合規版）:
  事後清潔在左側家族上是死路,任何形式都不行**。根因=刀鋒解（r41s1 已證 16 微擾合格保持 0):
  組拓撲不變 ≠ 電流路徑不變,brk 砍尖角=切關鍵電流節點。
- **推論（重要,待驗）**：深 lo 解可能**本質不可製造**——它們對 ±1px 全敏感,製造公差下大概率失效
  （對比:王朝系 x00 缺陷存活 72%）。可製造化只剩一條路=**生成端約束**（讓新解一開始就不靠
  對角連接/不長成刀鋒),不是事後修。

## 組文法生成系統（2026-07-26,Ricky 拍板）

- **方向**：把「組」從變異單元升級為**生成單元**——資料驅動的組文法取代手寫隨機
  （`_rand_blocks`/`_rand_frag`）;設計**多套候選文法、多輪測試後慢慢定案**;
  其他隨機生成器收編（分兩步:測試期降格為對照席,文法定案後全收;W 臂=變異臂不在收編列）。
- **普查基礎（2026-07-26,作戰區 7,011/合格 1,261）**：①**間距鐵律**——99.5% 組間距=貼 1 格縫
  （組是緊密打包不是散擺）;②合格解 84% 零對角（diagb P90=1）;③典型構成=主件 ~240px+
  中件 1.4+小件 1.2（金屬 ~400px）;④**質心熱圖高度離散**（家族祖傳格點）→忠實文法=家族
  複製機,位置軸必須加溫。
- **四候選**：G-A 忠實版（基準）/G-B 加溫版（位置均勻=反馬太）/G-C 可製造版（零對角硬約束+
  碎片度旋鈕=生成端可製造性+碎 vs 對角解耦）/G-D 左側仿生版（骨架+星座,零對角雙變體）。
- **判準（R43 起,D 臂 10 席=舊 2+四文法各 2）**：四尺=response 新穎度/誤差錨產率/
  苗子率(wm≥−3)/零對角 vs 帶對角 lo 分布;跨輪累積 ≥3 批判,不做單批判決;
  KPI=資訊增益,**不用三標率評**（探索系統,變現靠鏈）。

## 探索方向優先序（2026-07-29,Ricky 拍板「照你訂的順序」）

- **順序=①生成端知識化 → ②定向嫁接臂 → ③組圖表示（GNN bakeoff 準備）**。
- **①生成端知識化**：結構知識從「排序端」移「生成端」——per-組義容忍度統計（哪個組可動/往哪動/動多大,
  由既有組鏈量測資料重建）bias `_group_mutate` 變異分布。排序端路線已被反事實重播判死
  （d=1 鄰域包內 ρ 中位 0.068,SM 排的是自己的雜訊;見 discuss/audit-round2-2026-07-29.md）。
- **②定向嫁接臂**：「左側 lo 引擎 × 王朝肥沃骨架」系統性雜交（零件清單=組義字典;
  王朝已用樂透嫁接證明機制,改定向）。判準必含**嫁接體 lo 保留率**;觸發=c47d2 收鏈或 R48 開輪。
- **③組圖表示**：組圖轉換器（組義=節點/鄰接+對角=邊）先行準備;bakeoff 入場判準寫死=
  142 包 d=1 敏感度考卷（包內 ρ 中位 ≥0.30 ∧ pred_sd/real_sd ≥0.5）;pot 30k 觸發線不變。
- **理論背景**=盆地框架（Ricky 2026-07-29「推上去看運氣,系列上限就在那」）:爬山上限=盆地性質,
  嫁接=換盆地;鄰域肥沃度統一可爬升/可製造/穩健三軸。
- **附註（2026-07-29,Ricky「都修吧」批次）**：①--expert 撤案——R35「標配」廢止（反事實重播:SM 排序
  在 d=1 鏈鄰域 ρ 0.068=排自己的雜訊;flag 保留但不再使用）②audit 五修+analysis-07 三調全數落地
  （lr per-epoch/chain 發車驗錨+全出局記帳+wm>−2 組級包閘/_tri falsy/公證店前移/配套解析顯性化+
  manifest sm,heads 欄/adv 尺 n=0 判贏/組算子權重 C 帶化+grow k 2-3+gap≤2 加權）,詳 commit 記錄。

## 型態體系軸:負片(OOD)探索期評估法（2026-07-31,Ricky 拍板）

- **背景**:畢業水位已到（正片體系成果足額）,R50 起分**一半算力**給明顯分布外的生成體系——
  首選**負片/slot 體系**（連續金屬板挖洞,「起司」;Babinet 對偶=不同輻射機制,非視覺多樣性）。
  正片線照跑保底,負片純加碼=研究熱情,無畢業壓。
- **評估口徑（核心定案）**:**以 ≥10 round 的長 baseline 看**,明確**免疫於「長期沒過三標」**——
  現在是探索階段,三標/合格率**不是**負片臂的 KPI;停滯協議對負片臂的零變現**不觸發**。
  （既有「效率評 over ≥5 輪」的加強版:型態體系軸專用 ≥10 輪。）
- **負片臂真正的 KPI＝SM 域冷啟動曲線**:①OOD 凍結尺（首批切 ~15-20 筆永不入鍋）誤差 vs
  鍋內負片樣本數,從 n=0 記起;②前瞻 ρ（SM 首日起只記錄不採用=影子哲學）;③覆蓋擴張
  （lineage_pca 口徑,對全史最近距離）。「準」的操作型定義兩檔:ρ≥0.3=排序可用（SM 升顧問）/
  誤差 ≤ 正片 held-out 2×=編入漏斗。
- **科學價值**:負片資料起點=精確零（全史 27k 皆正片語彙）→ 第一次能完整量「代理模型對新域
  要多少資料才準」;正片 kpi.csv 全史當對照曲線。負結果（十輪全滅）亦立論=主流形態特殊性
  被 OOD 對照證實。
- **待辦（R49 批間閒時備料,R49 收輪+Ricky 點頭後發車）**:負片參數化生成器（板密度/洞形/縫長,
  決定性 seed）+Babinet 補集工具+R50 判準草稿含 OOD 凍結尺協議。
