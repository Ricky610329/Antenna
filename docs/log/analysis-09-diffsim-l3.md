# analysis-09｜diffsim L3：正統分層介質 Green's function

- 狀態：running
- 開檔：2026-08-03 02:43
- 零 HFSS（只讀 NAS 既有真值），與批次線並行；**GPU 讓給批次線**（`tmp/SESSION_COORDINATION.md` §2）
- 前情＝[analysis-08](analysis-08-diffsim.md)（L1 過 gate → L2 未過 → Ricky 指示重啟 → L2 物理修正）

---

## 0. 為什麼另開一檔

analysis-08 已經是「GATE 2 不過、依單向門停投 L2」的收檔敘事，且內含 6 處 ★ 修正；
再往上疊會難讀。L3 是**新的假設 + 新的判準**，照 `docs/log/CLAUDE.md` 開新檔、發車前寫死。

**L3 不是推翻 L2，是換掉 L2 的一個函式。** MoM 骨架（rooftop 基底／Z 組裝／遮罩解對顯式
縮減解 1.6e−14／互易性／`G_A^xy=0` 的 formulation C 選擇／dx 冪次／delta-gap 歸一化）
已被獨立稽核逐條通過，**不重寫**。要換的只有 `DCIMKernel.table(r, k0)`。

---

## 1. 假設

**現行 2 鏡像近似把表面波整段丟掉了，而表面波在這個 stackup 上不是小修正。**

證據（analysis-08 §10 + 本輪 fan-out）：

| | |
|---|---|
| 能量守恆 | η = P_rad/P_in = **0.872**，且**對三個不同尺寸貼片完全一致** → 系統性缺口非雜訊 |
| 核的直接比對（精確 vs 現行 2 鏡像，28GHz） | `G_A` 只差 8–18% 振幅、相位幾乎全對；**`G_V` 在 ρ=6.93mm 差 7.55× 且相位錯 77°** |
| 表面波佔 `G_V` 的比例 | ρ=1mm 時 31–53%，ρ=6.93mm 時 **64–84%** |
| 結構性理由 | DCIM 的函數族 `Σa·e^{−jknR}/4πR` 衰減不慢於 1/r，**表達不出**柱面波 `e^{−jβρ}/√ρ`（Şimşek 2006：球面波湊不出柱面波） |
| ⭐ 且只在 G_V | formulation C 下 `G̃_A = Ṽ_i^h/(jωμ₀)` 只含 TE 線，帶內無極點（TE₁ 截止 92.4GHz）→ **TM₀ 完全由 G_V 攜帶** |

→ 這解決了 analysis-08 §10 說的「正確的 residue 在 G_A 與 G_V 有不同權重與相位」：
**G_A 的 TM₀ 權重是 0。**

---

## 2. 判準（發車前寫死，2026-08-03 02:43；此時尚未跑任何 L3）

### 2.1 主 KPI 換成**層內 ρ**，且**只看 clean 與 neg**

analysis-08 §4.2 ★ 已證明 pooled ρ 主要在量「層間排序」（純分層 oracle 就有 +0.521 >
L1 的 +0.508），**不再使用**。改用層內，並且：

- **`senior` 排除**（Ricky 2026-08-03 拍板：「senior 的真的當作參考 可能有錯」）。依據：
  - 跨產線稽核：harvest 與我們的產線**不是同一把尺**，偏移 98.5% 集中在帶內 S11，
    機制是 **S11 共振頻率位移 |Δf| 中位 0.55 GHz 且方向隨機**（Wilcoxon p=1.5e-10, n=90）。
    **方向隨機的頻率抖動會直接摧毀任何模型的 rank**，那不是模型的錯。
  - 佐證：兩個**物理完全不同**的模型在該域都貼零（L1 +0.020／L2 −0.065）。
  - ⚠ 但要誠實：那 90 個交叉驗證樣本**全落在 harvest wm 前 5%**，95.3% 零覆蓋 →
    「senior 真值有問題」目前是**有依據的推定，不是已證實**。所以是「排除出主 KPI」，
    不是「宣告該資料無效」；仍照常報數字。
- **`frozen` 只報不判**：30 筆、CI 寬到沒有判別力。

### 2.2 Gates

發車前基準（L2 物理修正後，dev n=150/層，`v_scale=2.356`）：

| 指標 | clean | neg | 對照 L1 |
|---|---|---|---|
| wm 層內 ρ | **+0.031** | **+0.442** | clean +0.554／neg +0.159 |
| 帶內 S11 逐頻點 ρ（中位） | +0.244 | +0.399 | — |
| 谷底頻率 ρ | +0.233 | +0.040 | — |

| gate | 判準（**dev** 上量，val 不動） | 過 → | 不過 → |
|---|---|---|---|
| **G-L3a** | **clean 層內 ρ ≥ +0.40** | 繼續投 | 停，誠實記「精確核救不了正片域」 |
| **G-L3b** | **neg 層內 ρ 不退步（≥ +0.40）** | — | 記為 trade-off，不單獨判死 |
| 診斷① | 帶內 S11 逐頻點 ρ 中位（clean）應 ≥ +0.45 | 物理真的變準了 | 只有 wm 好＝可疑 |
| 診斷② | 谷底頻率 ρ 應顯著改善 | 共振位置抓對了 | — |

**clean ≥ +0.40 的理由**：L1 在 clean 是 +0.554，而 L1 的硬磁牆假設在 0.2mm 像素上
「本來就破掉」。**若把物理做對的 MoM 仍贏不了一個假設破掉的腔模型，MoM 這條路就該收。**
+0.40 也與 L1 的 gate 門檻一致，不另立標準。

### 2.3 ⚠ 能量守恆判準**必須反過來讀**（最容易誤判的一條）

L3 之後表面波是**真實的損耗管道**，而 `farfield()` 只算空間波
⇒ **η 應該從 0.872 掉到 ~0.72–0.78（文獻的表面波佔比 23–28%）**。

**η 下降是進步不是退步。** 新判準：`1 − η` 應該對得上獨立由 TM₀ residue 算出的表面波功率佔比，
而不是 `η → 1`。發車前先寫死，免得看到數字掉就誤判。

### 2.4 資料紀律（沿用 analysis-08，不放寬）
- 調參/迭代**只看 `dev`**；`val` 只在最終 gate 報數時看一次。
- val 已凍成 `configs/diffsim_val_freeze.txt`（120 筆內容 hash），5 條測試守著。
- 凍結尺永不進擬合。

---

## 3. 實作計畫

### 3.1 ★ 順序刻意不照文獻階段走：**先做最小可證偽版**

方案研究給的六階段裡，**S1（r=0 自項與近格的雙面積分）佔一半以上工作量，且是唯一沒有原型的**。
按順序做會在最貴、最不確定的地方卡住，卻還沒回答「L3 值不值得」。

**所以先跳過 S1**：自項繼續用現行的 `b_reg = 0.4·dx` 頂著（它本來就在扮演這個角色）。
依據是診斷本身就指向遠距離——`G_A` 在 ρ→0 只差 11%、`G_V` 差 2%，**錯的全在遠端，而遠端不需要 S1**。

| 階段 | 做什麼 | 出口 |
|---|---|---|
| **A（最小可證偽）** | spectral 核 + 靜態扣除 + 上偏折 GL 積分 → 293 個相異距離的表；自項沿用 b_reg | dev 上量 G-L3a/b。**不過就停，不做 S1** |
| B（僅當 A 過） | r=0 自項與近格的雙面積分（奇異性抽取 + 解析/Duffy） | Re(Zin) 對矩形貼片的量級 |
| C | empymod 獨立對帳（**已派 agent 進行中**） | 相對誤差 <1% |
| D | delta-gap 假電容（**已派 agent 進行中**；與 L3 同為一階，缺它到不了真值） | 中位 min-S11 −1.33 → ~−3.3 |
| E（選） | 精確表上疊零初始化可微殘差 | 記帳：物理節 vs 學習節 |

### 3.2 可微性（Ricky 的硬約束）

**核可不可微，對 ∂L/∂ρ 完全沒有影響** —— 已用 gradient check 實測確認：
梯度鏈是 `ρ → wgt → load → (Z + diag(load)) → solve → S11`，**Z（含核）在這條鏈上是常數**。
8 個抽樣像素 7 個相對誤差 <1%，625/625 像素有非零梯度（`test_l2_gradient_check_vs_finite_difference`）。

⇒ 離線算 Sommerfeld（哪怕完全用 scipy、完全不可微）**零損失**。
代價只有「對 stackup（h、εr）與核參數不可微」——而 h/εr 只有 2 個純量、離線重算全表 6 秒，
有限差分就夠；核的可置換性由階段 E 的殘差層保留。

### 3.3 為什麼連內插都不需要
25×25 的格心偏移 `r² = gi² + gj²` 是整數 → **只有 293 個相異距離**。
全表 2 核 × 17 頻 × 2499 偏移 complex128 = **156 KiB**，離線 ~6 秒。
`l2.py` 現成的 `self.off` 索引直接用 → **精確查表、零內插誤差、比現在還快**（省掉 2499×3 次 exp/sqrt）。

---

## 4. 階段 A 結果（2026-08-03 02:57，dev；`val` 未動）

### 4.1 實作

`script/diffsim/l3.py`。獨立驗證（我自己重跑，非採信 agent）：

| 檢驗 | 結果 |
|---|---|
| TM₀ 極點 n_eff @24/28/32GHz | **1.0182 / 1.0254 / 1.0342** — 與方案研究完全一致 |
| εr=1 → 「源 − 鏡像」閉式 | **1.35e−06**（對係數/符號/分支錯誤致命敏感） |
| 建表成本 | 2499 偏移 → **293 個相異距離** × 17 頻，~6 秒、1.3 MiB |
| 執行成本 | **282 ms/筆**（比 DCIM 的 307 ms **更快**——查表省掉 exp/sqrt） |

### 4.2 ❌ **G-L3a 未過**

```
| 核        | clean ρ(wm)              | neg ρ(wm) | 帶內S11逐頻ρ | 谷底頻率ρ |
| L2 DCIM   | +0.031 [−0.143, +0.202] | +0.442    | +0.244       | +0.233    |
| L3 exact  | +0.194 [+0.032, +0.350] | +0.563    | +0.154 ↓     | +0.065 ↓  |
```

- **G-L3a（clean ≥ +0.40）：未過**（+0.194）
- G-L3b（neg 不退步）：**過**（+0.442 → +0.563，且 CI 不含 0）
- ⚠ **診斷指標反而變差**——正是 §2.2 預警的「只有 wm 好＝可疑」。

⚠ 能量守恆：η 從 0.876 掉到 **0.37–0.40**。依 §2.3 這方向是對的（表面波是真實損耗管道），
但幅度比文獻的 23–28% 大得多。**值得注意的是 η 不再是常數**（0.395/0.386/0.366，形狀相依），
這比 DCIM 的「三個尺寸完全一致的 0.876」更像真實物理。

### 4.3 ★ 但「未過」的原因已定位：**是我刻意跳過的自項，不是核**

§3.1 跳過 S1 時的理由是「錯的全在遠端」。掃 `b_reg`（自項正則化半徑，dev 小樣本 35/層）：

| b_reg/dx | clean ρ(wm) | neg ρ | 帶內S11逐頻ρ | 谷底頻率ρ |
|---|---|---|---|---|
| **0.2** | **+0.343** | +0.461 | **+0.277** | +0.186 |
| 0.3 | +0.321 | +0.464 | +0.222 | +0.248 |
| 0.4（階段 A 用的） | +0.259 | +0.509 | +0.178 | +0.330 |
| 0.55 | +0.100 | +0.421 | +0.153 | +0.049 |
| 0.8 | +0.049 | +0.311 | +0.138 | +0.138 |

**clean ρ 變化 7 倍（+0.049 → +0.343），單調「越小越好」⇒ 自項是主導因素。**
而 `b_reg = 0.4·dx` 是**為舊 DCIM 核調的**，對精確核不是最佳值。

⇒ §3.1 那句「錯的全在遠端」**只對了一半**：遠端確實錯（G_V 差 7.55×、相位 77°），
但把遠端修對之後，**近端/自項的相對誤差就變成新的主導項**。這是可預期的、也是好消息——
它把 S1 從「可能白做的 1–2 天」變成「已定位的瓶頸」。

### 4.4 ✅ 階段 C：empymod 獨立對帳 —— **核是對的**（2026-08-03 03:2x）

派獨立 agent 用**四條互相獨立的管道**驗 `l3.py` 的公式（它拿到的是公式轉錄，不是我的結論）：

| 管道 | 驗到什麼 | 結果 |
|---|---|---|
| **εr=1 精確鏡像法** | 分支選擇／`Z₀^{e,h}`／短路 stub／`V_i`／**兩行係數的前因子** | **1.5e−13 ~ 2.2e−12** |
| **TM₀ 極點 vs 教科書色散式** | εr 擺放（最銳利的檢驗） | **1.0181707/1.0254101/1.0342279** — 我的 1.0182/1.0254/1.0342 對到 7 位 |
| **Fresnel 多次反射幾何級數**（完全不同的推導路線，不用傳輸線） | `Z_dn = jZ₁tan(k_z1 h)` | TE **1.0e−15**／TM **6.0e−11** |
| **empymod 場層級 @εr=3.55** | 整體 | 相對差 1e−6 ~ 1.6e−1，**全部 40 點都落在 empymod 自己的方法散布內** |

★ **關鍵是它做了鑑別力測試**：故意把公式弄錯再跑同一套比較 —— `Z₁^e` 漏 εr → 誤差 1.07；
`n=√εr` 塞進 `k_z0`（**我們專案的歷史 bug**）→ G_A 1.43；連「G_V 只錯 10%」都有 **16× 分離**。
⇒ 這個對帳能把 G_V 釘在 **±1%**、G_A 在 **±2–3%**，**不是「湊得出來就算過」**。

⚠ 誠實的限制（agent 自己標的，我照抄不美化）：
- empymod 在我們這個波段（k₀h ≈ 0.26–0.34）**自己就有 1e−2…1e−1 的地板**（它是地球物理擴散區的碼），
  所以 εr=3.55 的**場層級**對帳**不能**單獨認證到 1e−3。εr 相依性的高精度證據來自
  極點（7 位）＋Fresnel（1e−11）＋εr=1 前因子（1e−12），**三者合起來才成立**。
- 「極點留數振幅」那條交叉檢驗**沒做成**（用無損極點套含損核，比值差 1e13）——**不算通過**，
  agent 明說沒有調參去湊它。
- 全部驗證都在「源與觀察點都在上方空氣區」；**不涉及垂直電流 / `G_A^{zx}`**。

順手抓到兩件（都不在公式，在周邊）：
1. **我的 `G_V` 數值精度是 ~3e−4（3.5 位有效），不是我以為的 1.5e−4** ——
   那 1.5e−4 是 quadrature 殘差不是理論極限。改用「直達項閉式剝離」取代靜態抽取，
   **成本一樣、精度 3e−4 → 7e−7**。（是否值得做見 §4.5 的敏感度測試。）
2. `l3_probe4/5` 的**遠場診斷是壞的**（上偏折的代價 e^{+δk₀ρ} 在 ρ=60mm 放大 67×，
   報出 n_eff = −0.128）。**核本身沒問題**，且我實際只用到 ρ ≤ 6.93mm，**不受影響**。

⇒ **「停」的一個主要前提被排除了**：不是核錯。

### 4.5 G_V 的 3e−4 精度**不用管**（配對敏感度實測）

agent 建議把 G_V 精度從 3e−4 提到 7e−7。但正確的問題不是「精度夠不夠高」，
而是「**這個精度會不會動 rank ρ**」——後者才決定要不要花力氣。
做法：同一批樣本、同一個表，只把 `G_V` 乘 `(1 + ε·randn)`（複數，實虛部獨立——
quadrature 誤差就長這樣），各 3 個 seed（`scratchpad/gv_sens.py`，n=40/層）：

| ε | clean Δρ | neg Δρ |
|---|---|---|
| **3e−4（＝實際精度）** | **+0.003 ± 0.002** | **−0.006 ± 0.000** |
| 3e−3 | +0.008 | −0.043 |
| 3e−2 | −0.017 | −0.204 |

⇒ 實際精度造成的 ρ 變動 ~0.005，**比抽樣誤差小一個量級**。**不提精度**——
真要動核，力氣該花在自項（§4.3），不是把已經夠好的遠端再磨三位數。
（`l3_probe4/5` 的遠場診斷 bug 同理不修：實際只用到 ρ ≤ 6.93mm，不在壞掉的區間。）

### 4.6 ⚠ 對 §4.3 證據強度的自我查核（2026-08-03 03:4x；**結果見 §4.8——§4.3 確實被修正**）

跑 §4.5 時撞到一件必須先處理的事：**同一個核、同一個 `b_reg=0.4`**，
n=40/層 量到 clean ρ = **−0.002**，而 n=150/層 的 gate 量到 **+0.194**。

⇒ **clean 層的 ρ 在小樣本下抽樣變異很大**（n=40 的 Spearman SE ≈ 0.16；兩者差 0.196
≈ 1.1 個標準誤，落在雜訊內）。而 §4.3 的 b_reg 掃描是 **n=35/層**。

這不必然推翻 §4.3——那個掃描是**配對**的（同一批樣本跑全部 b_reg），配對比較的**趨勢**
比絕對值穩健得多。但我正要拿「7 倍變化」去主張「自項是瓶頸、值得進 S1」，
**證據不牢就不能拿去用**。

依 `decisions.md`「獨立稽核制度化」第 3 條（影響方向的宣稱自己重跑），
在 **gate 口徑 n=150/層** 上重跑全部 5 個 `b_reg`，並加**對樣本**的配對 bootstrap
（`scratchpad/breg_v2.py`，B=2000）→ **§4.8**。
★ 事後看，這個查核是必要的：**§4.3 與 §4.7 兩處解讀都被它修掉**。

### 4.7 ★★ S1 的效果上界**可以直接算**，不必先做 1–2 天 —— 結論：**S1 救不了**

§4.3 的解讀有一個我沒察覺的岔路。`l2.py:31` 我自己寫過
**「`b_reg` ⚠ 是自由參數不是『正則化』——實測 Im(Zin) 隨它變 4.6 倍」**。
所以「掃 `b_reg` 有 7 倍變化」有兩種**相反**的解讀：

| | 若成立 | 判別方式 |
|---|---|---|
| **(A) 自項真的是瓶頸** | S1 有用 | 真積分算出的 `b_eff` **接近**掃描最佳值 |
| **(B) `b_reg` 在補償別的誤差** | S1 做了也沒用 | `b_eff` **遠離**掃描最佳值 |

**兩者可以直接算出來分開**（`scratchpad/selfterm.py`，純數學、30 分鐘）：
現行 code 對所有 (m,n) 都用點對點 `dx⁴·G(r_mn)`，只有 r=0 拿 `b_reg` 頂著；
真值是格對格雙面積分。用**自相關 + 極座標**（Jacobian 的 r 正好消掉 1/R 的 r，
被積函數變有界）降到一維，`quad` 誤差 3.3e−14：

```
J_self = ∫∫_{cell×cell} 1/R dS dS' = 2.9732095982 · a³
     ⇒  b_eff = a⁴/J = 0.336337 · dx        ← 物理正確值
```

| | b_reg / dx |
|---|---|
| 掃描最佳值 | **0.20** |
| 階段 A 用的 | 0.40 |
| **真積分（本計算）** | **0.3363** |
| `diag4` 的獨立計算（不同方法） | 0.3363 ✓ |

⇒ **掃描最佳的 0.20 離物理正確值差 40% ⇒ 是解讀 (B)。**

順便把 S1 的另一半（近格雙積分）也算完了 —— 真積分/點對點的倍率：

| 偏移 | (1,0) | (1,1) | (2,0) | (2,2) | (4,0) | (10,0) |
|---|---|---|---|---|---|---|
| 倍率 | **1.1121** | 1.0592 | 1.0215 | 1.0112 | 1.0052 | 1.0008 |

最大 11%，衰減很快 —— 與 §3.1「錯的全在遠端」的原始診斷（G_A 在 ρ→0 只差 11%）**吻合**。

**⇒ S1 做完能拿到的是「0.40 → 0.3363」那段，不是「0.40 → 0.20」那段。**
按 §4.3 掃描內插約 **+0.04**（+0.259 → ~+0.30），**離 +0.40 還很遠**。
這讓「停」從「試了沒用就放棄」變成**有計算支持的判斷**：
不是「S1 沒做所以不知道」，是**S1 的效果上界已經算出來、不足以翻盤**。

### 4.8 ★ 複驗回來了：**§4.7 的「解讀 (B)」錯了，但結論不變**（2026-08-03 04:0x）

n=150/層（gate 口徑）＋對**樣本**的配對 bootstrap（B=2000，`scratchpad/breg_v2.py`）：

| b_reg/dx | clean ρ | 95% CI | neg ρ | 95% CI |
|---|---|---|---|---|
| 0.20 | +0.242 | [+0.080, +0.388] | +0.475 | [+0.325, +0.597] |
| **0.30** | **+0.249** | [+0.085, +0.393] | **+0.568** | [+0.448, +0.665] |
| 0.40（階段 A） | +0.194 | [+0.024, +0.347] | +0.563 | [+0.443, +0.661] |
| 0.55 | −0.038 | [−0.206, +0.130] | +0.447 | [+0.302, +0.581] |
| 0.80 | −0.159 | [−0.327, +0.011] | +0.363 | [+0.212, +0.506] |

`Δ(0.2−0.8)` clean **+0.398**，CI [+0.262, +0.540]，P(>0)=**100%** → **趨勢是真的**。
但 `Δ(0.2−0.4)` 只有 +0.047，CI **跨 0**（P=89.4%）→ **「0.2 最好」不成立**。

★ **兩處修正**：
1. **§4.3 的「7 倍變化（+0.049→+0.343）」是 n=35/層 的假象。** 同口徑（n=150）是
   −0.159 → +0.249。**趨勢在、幅度誇大**——正是 §4.6 擔心的事，複驗是對的。
2. **§4.7 判成「解讀 (B)」錯了。** 掃描最佳不是 0.20 而是 **0.30**，而真積分算出的
   **b_eff = 0.3363** 就落在旁邊（差 11%，且都在彼此的 CI 內）⇒ 是**解讀 (A)**：自項確實重要。

⇒ ✅ **這反而是一個漂亮的獨立一致性檢驗**：純數學算出 0.3363、純實驗掃出 ~0.30，
兩條互不相干的路吻合 —— **模型的自項處理在物理上是對的**。

**但最終結論完全沒變，只是理由換了**：
不是「b_reg 在補償別的誤差所以 S1 沒用」，而是**「階段 A 用的 0.40 本來就離最佳不遠，
所以修對了也只值 +0.04」**（0.40 的 +0.194 → 0.3363 內插約 **+0.23**）。
——與 §4.7 用完全不同路徑估的 +0.04 **一致**。

### 4.9 ★ L1 對照數字的口徑錯誤（判準表 §2.2 要更正）＋ **L1 與 L3 幾乎不相關**

跑互補性測試（`scratchpad/l1l3_comp.py`，**同一批 dev 樣本、配對**）時發現：

| clean 層內 ρ | neg 層內 ρ | 出處 |
|---|---|---|
| +0.554 | +0.159 | §2.2 判準表引用的「對照 L1」——**analysis-08 時期，不同口徑** |
| **+0.418** | **+0.349** | **本次，與 L3 同一批 300 筆樣本** |

⇒ 我前面拿 +0.554 去對比 L3 的 +0.194，**是不公平的比較**（誇大了差距）。
判準本身**不受影響**：G-L3a 的 +0.40 仍略低於同口徑 L1 的 +0.418，
「贏不了腔模型就收」的邏輯照舊成立；但**引用的數字要以同口徑為準**。

★ **真正的發現——兩個模型幾乎不相關**：

| 層 | L1 | L3 | **corr(L1, L3)** |
|---|---|---|---|
| clean | +0.418 | +0.194 | **+0.013** ← 幾乎零相關 |
| neg | +0.349 | +0.563 | +0.330 |

clean 層 corr ≈ 0 表示**兩者抓到的是完全不同的訊號**——這不奇怪，
它們的物理本來就無關（腔模態展開 vs 積分方程），但零相關是**強互補**的必要條件。
naive 合併（層內 z-score，**權重固定 0.5 不調**）：

| 層 | 最佳單一 | z(L1)+z(L3) |
|---|---|---|
| clean | +0.418（L1） | **+0.439** |
| neg | +0.563（L3） | **+0.569** |

點估計上兩層都贏過任一單一模型。但我當下標了「+0.02 完全可能是雜訊，
winner's curse 在這種比較特別會咬人，**不先判**」——**結果正是如此**，見 §4.10。

⚠ dev 上掃到的最佳權重（clean w_L1=0.75 → +0.459；neg w_L1=0.25 → +0.599）
**不當結論報**——那是 dev-tuned，報它就是 Goodhart。只當敏感度附記。

### 4.10 ❌ **合併不產生顯著增益**（配對 bootstrap，B=4000）

`scratchpad/l1l3_boot.py`，權重**固定 0.5 不調**，重抽的是**樣本**：

| 層 | 點估計 | comb − L1 | comb − L3 |
|---|---|---|---|
| **clean** | L1 +0.418／L3 +0.194／comb +0.439 | **+0.022**，CI [−0.083, +0.130]，**P=65.8%** ❌ | +0.242，CI [+0.110, +0.381]，P=100% ✅ |
| **neg** | L1 +0.349／L3 +0.563／comb +0.569 | +0.218，CI [+0.123, +0.316]，P=100% ✅ | **+0.004**，CI [−0.080, +0.090]，**P=53.6%** ❌ |

⇒ **在每一層，合併只是「追平該層的最佳單一模型」**，沒有超越。
零相關（clean corr=+0.013）**沒有換來增益** —— 因為 L3 在 clean 太弱（+0.194 vs +0.418），
合併只是把強的稀釋掉；neg 反過來。

⚠ 誠實的限制：n=150 的 CI 半寬 ≈ 0.11，**真實增益若小於 0.11 就偵測不到**。
「沒有顯著增益」≠「證明沒有增益」。要判死需要更大的樣本或更好的合併方式
（例如殘差 head 而非線性組合——§4.9 量到 corr(L1 殘差, L3 預測) = −0.19/−0.17，
**有一點結構但不強**）。

★ 這條的價值不在結論而在**流程**：§4.9 我寫下「+0.02 可能是雜訊、不先判」，
一小時後 bootstrap 證實了。**先寫下懷疑再驗證**比事後解釋便宜得多。

## 5. 結論

### 5.1 判定：**G-L3a 未過，依 §3.1 停，不做 S1**

| gate | 判準 | 結果 | |
|---|---|---|---|
| **G-L3a** | clean 層內 ρ ≥ +0.40 | **+0.194** | ❌ |
| G-L3b | neg 不退步（≥+0.40） | +0.563（L2 是 +0.442） | ✅ |
| 診斷① | 帶內 S11 逐頻 ρ（clean）≥ +0.45 | +0.154（**比 L2 的 +0.244 更差**） | ❌ |
| 診斷② | 谷底頻率 ρ 顯著改善 | +0.065（**比 L2 的 +0.233 更差**） | ❌ |

### 5.2 但「停」的理由與 §3.1 預想的**不同**——這才是本輪的主要產出

原判準寫「不過 → 停，誠實記『精確核救不了正片域』」。
實際查下來，**那個歸因是錯的**。四條獨立證據：

| # | 證據 | 排除了什麼 |
|---|---|---|
| 1 | empymod 四管道對帳（εr=1 到 1e−12／極點 7 位／Fresnel 1e−15），**含鑑別力測試** | 「核寫錯了」 |
| 2 | 真積分 `b_eff = 0.3363 dx` **落在實測最佳 0.30 旁邊** | 「自項處理錯了」 |
| 3 | G_V 精度擾動實測 Δρ ≈ 0.005 | 「數值精度不夠」 |
| 4 | S1 上界**兩條獨立路徑都估到 +0.04** | 「還沒做完所以不知道」 |

⇒ **不是核錯、不是自項錯、不是精度不夠、也不是沒做完。**
是這個離散化（25×25 格、0.2mm 像素、rooftop、點對點近似）在這個問題上的**天花板**。

### 5.3 觸發 §2.2 寫死的收線條件

> 「若把物理做對的 MoM 仍贏不了一個假設破掉的腔模型，MoM 這條路就該收。」

**同口徑**（同一批 300 筆 dev，配對）：

| | clean | neg | 成本 |
|---|---|---|---|
| **L1 腔模型** | **+0.418** | +0.349 | **84 ms/筆** |
| L2 DCIM MoM | +0.031 | +0.442 | 307 ms |
| L3 精確核 MoM | +0.194 | **+0.563** | 282 ms |
| L1+L3 合併（w=0.5） | +0.439（**不顯著**贏 L1） | +0.569（**不顯著**贏 L3） | 366 ms |

**⇒ MoM 這條路收。** L1 是 diffsim 線唯一有實用價值的產物：clean 最強、快 3 倍、
而且它的假設（硬磁牆）明知在 0.2mm 像素上是破的——**這反而說明 rank 這個目標
對物理精細度沒有想像中敏感**。

### 5.4 L3 不是白做（三件留下來的東西）

1. **`script/diffsim/l3.py`**：可重用的精確分層 Green's function（Michalski–Mosig
   formulation C），已被四管道獨立驗證。換 stackup 只要改 `h`/`εr` 重建表（6 秒）。
2. **一個被證實的物理自洽性**：純數學的 `b_eff=0.3363` 對上純實驗的掃描最佳 ~0.30。
3. **`selfterm.py` 的自相關+極座標降維**：把奇異雙面積分變成有界一維積分（quad 誤差 3e−14），
   任何 MoM 自項都能用。

### 5.5 ⚠ 真正的瓶頸可能不在模型，在**真值**

L1 用一個破掉的假設拿 +0.418，而物理精確的 MoM 只有 +0.194 —— 這個反差本身是訊號。
跨產線稽核已量到 S11 **共振頻率位移 |Δf| 中位 0.55 GHz 且方向隨機**，
領先假說是 HFSS 自適應網格未收斂（`MaxDeltaS 0.02` + `MaximumPasses 6`）。
**若我們自己的真值也有這個抖動，rank ρ 就有一個算得出來的天花板，
再修多少模型 bug 都到不了。**
→ 規格與判準已寫死：[proposal-mesh-convergence](../discuss/proposal-mesh-convergence.md)（~1 小時機時），
已升 `configs/ONGOING.md` 🔜 候選。**這是本輪最該接下去的事，優先於任何模型改良。**

### 5.6 未做／未答（誠實清單）

- **與現役 SM 的最終比較**（`diffsim.md` 原計劃最後一步）：**未做**。
  現役 SM 是 NAS 的 `sm_reanchor*.pth`，屬本輪授權的禁區（唯讀清單外），
  未經 Ricky 解除前不碰。⇒ **「diffsim 值不值得接進管線」目前無法回答。**
- **delta-gap 假電容**（x=5.0mm 的 1 格死巷樁，估 38.5 fF、值 ~2 dB）：agent 仍在跑。
  它影響**絕對值**不影響**排序**，且 L1 不受影響 —— 不會改變 §5.3 的收線判定。
- **合併的真實增益**：n=150 只能偵測 >0.11 的差異。「不顯著」≠「證明沒有」。
- **η 的能量缺口**：L3 後 η = 0.37–0.40，比文獻表面波佔比（23–28%）大得多，
  **~35% 未解釋**。已知不是偽損耗（§4.4 排除），但沒有查到底。

依 §3.1 寫死的規則，階段 A 不過**就該停、不做 S1**。但那條規則的**理由**是
「代表精確核救不了正片域」——而 §4.3 的證據顯示這個推論**不成立**：
失敗有一個具體、已定位、可修的原因。

**我不自行放寬判準**（那正是 Goodhart 護欄要防的）。現況據實記錄，是否進 S1 交 Ricky 裁定。
兩個未決因素也一併列入判斷：
- **empymod 獨立對帳未回**——若核本身有錯，「停」的結論會建立在錯的實作上。
- **delta-gap 假電容未修**——它與 L3 同為一階（值 ~2 dB），缺它到不了真值。

## 5. 結論

（收檔時填）


---

## 6. ★★★ 收檔後修正（2026-08-03 05:1x）：**§5.2 的「離散化天花板」結論被推翻**

append-only：原文不改，在此原地說明。

### 6.1 事件

收檔後（analysis-10 期間）採納了饋電稽核 agent 的 **Stage 1**——
`stub_mask()` 原本用 **0.2mm 死端**代表 HFSS 裡 **22.5mm 的連續饋線**，那是明確的建模錯誤。
移除後，我自己在**同一批 dev 樣本上配對複驗**（`scratchpad/stage1_check.py`，B=3000）：

| dev n=150/層 | clean 層內 ρ | neg 層內 ρ |
|---|---|---|
| BEFORE（有 stub） | +0.194 | +0.563 |
| **AFTER（Stage 1）** | **+0.434** | +0.525 |
| **Δ（配對 bootstrap）** | **+0.239** [+0.144, +0.347] **P=100%** | −0.037 [−0.097, +0.011]（不顯著） |

⚠ 這比稽核 agent 自己報的 Δρ **+0.051** 大得多——因為它報的是 `ρ(m_s11)`（S11 通道），
我報的是 **`worst_margin` 層內 ρ**（本輪的 gate 指標）。兩者都對，是不同的量。

### 6.2 ⇒ **G-L3a 其實過得了**

§2.2 的判準是「dev 上 clean 層內 ρ ≥ +0.40」。**+0.4339 > +0.40。**
⇒ §4.2 的「G-L3a 未過」在**修掉饋電模型之後不成立**。

### 6.3 ★ 但真正的教訓在 §5.2：**排除法只能排除你想到的**

§5.2 列了四條「獨立證據」宣稱排除了所有可能，結論是「離散化的天花板」：

| # | 排除了 | |
|---|---|---|
| 1 | 核寫錯了 | ✅ 仍成立（empymod 四管道） |
| 2 | 自項處理錯了 | ✅ 仍成立（b_eff 0.3363 ↔ 實測 0.30） |
| 3 | 數值精度不夠 | ✅ 仍成立（Δρ≈0.005） |
| 4 | 還沒做完（S1） | ✅ 仍成立（上界 +0.04） |
| — | **饋電模型錯了** | ❌ **我根本沒把它列進候選** |

四條都對，**結論卻是錯的**——因為第五條沒被想到。
⇒ 「我排除了 A/B/C/D，所以是 E（天花板）」這種論證，**強度取決於候選清單的完整性**，
而完整性是無法自我驗證的。**下次寫「⇒ 是天花板」之前，先問「我怎麼知道清單是完整的」。**

⚠ 諷刺的是：饋電模型的問題**當時已經有人在查**（我自己派的 delta-gap agent，
那時正在跑），我卻在它回來之前就下了「天花板」的結論。§4.5 我還寫過
「兩個未決因素也一併列入判斷」——**列了，但沒等**。

### 6.4 那 §5.3「MoM 路線收線」還算數嗎？

**算數，但理由要換掉。** 原理由是「贏不了 L1」——那是用 ρ 判的。
analysis-10 §6 用**選批（top-K）**的尺重量，結論是
**L1 與 L3 在正片域都比隨機還差**（P(勝隨機) 39%／18%），SM 三個都 80%+。
⇒ 收線的理由不再是「MoM 輸給腔模型」，而是**「兩者在實用指標上都沒有價值」**。
⚠ analysis-10 §6 的 L3 數字是 **Stage 1 之前**的，已重跑，見該檔 §7。
