# 事實宣稱台帳

> 標籤定義見 [README.md](README.md)。行號基準 commit `577d7ce`（2026-08-31）。
> **進度**：第一批＝2026-09-01 當場查證的條目（C-01…C-12）。R50–R79 補帳未完成，見文末待辦。

---

## 一、對「什麼有效」的核心宣稱

| ID | 宣稱 | 證據 | 狀態 | 域 | 出處 | 移植 |
|---|---|---|---|---|---|---|
| C-01 | **選拔（用既有量測資料庫挑起點）是最大單一槓桿**：+3.63 dB vs 零演算法基線 | `A` | ✅現行 | dual | `round-71-dual-dynasty-mining.md`:26-28,69（**hc 對照臂＝均勻隨機抽 harvest+標準橋，零選拔零開採**） | 帶走（結構） |
| C-02 | **局部開採（鄰域爬山）貢獻 +2.08 dB**，是第二大槓桿 | `A` | ✅現行 | dual | 同上 `:28,69`（開採增益＝王 −3.82 vs A_11 起點 −5.90） | 帶走（結構） |
| C-03 | 四個現行紀錄的血統**全部**來自鄰域開採／代理目標開採線／分歧臂 | `B` | ✅現行 | dual | `records_dual.json` 各鍵 note：`wm_dual`＝sel 開採線、`m4_stop_s21`＝m4_king 鄰域、`wm_full`＝sel 家族 d 臂、`wm_mfg`＝grind_loop d 分歧臂 | 帶走（結構） |
| C-04 | **SM 排序在域內顯著勝過隨機**：P(勝隨機) 80%+ | `B` | ✅現行 | dual | `analysis-09-diffsim-l3.md`:506（同表 L1 39%／L3 18%＝比隨機差） | 帶走（結構） |
| C-05 | **批次篩選的效率優於 SM-in-the-loop 線上迴圈**（單位樣本推進：批次優） | `A` | ✅現行 | dual | `round-73`（README 索引 :56：「基建落地零 error 但效率輸批次」） | 帶走 |
| C-06 | **排序用 margin 頭優於「先預測響應再取極值」**：ρ 0.51 vs 0.39 | `A` | ✅現行 | dual | `round-58-dual-sm-coldstart.md`:82；實作 `sm_dual.py:376-388,448,524-529`；對照組保留在 `cmd_eval` `:656-659` | 帶走（發現） |

## 二、對「什麼無效／要修正」的宣稱

| ID | 宣稱 | 證據 | 狀態 | 域 | 出處 | 移植 |
|---|---|---|---|---|---|---|
| C-07 | **主 SM 在完全未見域選批比隨機還差**：P(勝隨機) 21–26%（K=30/60 皆然） | `A` | ✅現行 | 通用 | `analysis-10-sm-vs-physics.md` §5.2 表、§5.3 第 2 點；已升 `decisions.md`「驗收指標…」節與 scratch:632 | **帶走（鐵則）** |
| C-08 | **`rank ρ` 與 top-K 選批效益不等價，甚至反向** | `A` | ✅現行 | 通用 | `analysis-10` §5.3 第 3 點（ρ=+0.433 的 L3 只有 30%；ρ=+0.078 的影子 CNN 有 71%）；diffsim 案例：ρ 改善 +124% 而 P(勝隨機) 18%→17% | **帶走（鐵則）** |
| C-09 | 「線上學習（generator 梯度路）是主線」 | `A` | ⛔已證偽→降格為工具箱一員 | 通用 | `decisions.md`:75-82、:252-266、:880；`configs/dual_r1_eval.yaml`:3（批次線不讀線上欄位）；C-05 | 留下 |
| C-10 | 「agent 在搜尋迴圈之內」 | `A` | ⛔已證偽 | harness | `grind_loop.py`:1-18 **「迴圈裡沒有 LLM」**，而現任王 `smp073_d_040` 正是它出品（`records_dual.json` wm_mfg note） | 修正後帶走 |

## 三、未驗證但被當成事實使用的宣稱

| ID | 宣稱 | 證據 | 狀態 | 域 | 出處 | 移植 |
|---|---|---|---|---|---|---|
| C-11 | 「換一個模型接手會得到同一份帳」（弱模型化的核心賣點） | `–` | ❓未驗證 | harness | `methodology.md` §8.3 自陳「**這是一個論證，不是一個實驗結果。本研究沒有做過『更換執行模型、重跑同一輪』的對照**」；`methodology-divergences.md` S3 已降級 | 待決（先驗證） |
| C-12 | 「制度使人與 agent 都不能繞過」 | `–` | ↩已撤回→「繞過會**留下紀錄**」 | 通用 | `methodology-divergences.md` S4：制度載體是雙方都可寫的檔案，無權限／簽核／稽核機制 | 帶走（修正版） |

---

## 待辦：R50–R79 補帳（本卷宗最重的一塊）

三份綜合層文件都停在七月底，以下區段**從未被綜合過**，需逐輪建帳：

| 區段 | 內容 | 為何重要 |
|---|---|---|
| R50–R56 | 型態體系軸元年、低對角左側軸、微調期量測三紀律、R54 菱形橋定案 | 可製造性規範的來源（送板交付直接依賴） |
| **R57–R69** | **dual 線開局至幾何儀器換代 p01**：開局三原則、分歧採樣、六軸尺、規格 v2、哥白尼輪（p00 對角零厚度＝數值幻影） | Ricky 認為最有價值的「快速收斂」就在這段 |
| R70–R79 | 可製造時代（wm_mfg 開帳 −5.90 → −2.39）、多起點、家族極限量化、保和再平衡、grind_loop 無人值守 | 現任王的產生機制；大量「定讞」結論 |

補帳方式：逐輪讀 `docs/log/round-NN-*.md` 的 §4/§5，**只收有對照或有公證的數字**，其餘進 `ceilings.md`。
